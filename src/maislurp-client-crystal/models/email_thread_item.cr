# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  class EmailThreadItem include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: itemType, type: String)]
    property item_type : String


    @[JSON::Field(key: entityId, type: String)]
    property entity_id : String


    @[JSON::Field(key: bodyExcerpt, type: String, nilable: true, emit_null: true)]
    property body_excerpt : String


    @[JSON::Field(key: textExcerpt, type: String, nilable: true, emit_null: true)]
    property text_excerpt : String


    @[JSON::Field(key: subject, type: String, nilable: true, emit_null: true)]
    property subject : String


    @[JSON::Field(key: to, type: Array(String))]
    property to : Array(String)


    @[JSON::Field(key: from, type: String, nilable: true, emit_null: true)]
    property from : String


    @[JSON::Field(key: bcc, type: Array(String), nilable: true, emit_null: true)]
    property bcc : Array(String)


    @[JSON::Field(key: cc, type: Array(String), nilable: true, emit_null: true)]
    property cc : Array(String)


    @[JSON::Field(key: attachments, type: Array(String), nilable: true, emit_null: true)]
    property attachments : Array(String)


    @[JSON::Field(key: createdAt, type: Time)]
    property created_at : Time


    @[JSON::Field(key: read, type: Bool)]
    property read : Bool


    @[JSON::Field(key: inReplyTo, type: String, nilable: true, emit_null: true)]
    property in_reply_to : String


    @[JSON::Field(key: messageId, type: String, nilable: true, emit_null: true)]
    property message_id : String


    @[JSON::Field(key: threadId, type: String, nilable: true, emit_null: true)]
    property thread_id : String


    @[JSON::Field(key: sender, type: Sender, nilable: true, emit_null: true)]
    property sender : Sender


    @[JSON::Field(key: recipients, type: EmailRecipients, nilable: true, emit_null: true)]
    property recipients : EmailRecipients

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@item_type : String, @entity_id : String, @body_excerpt : String | Nil, @text_excerpt : String | Nil, @subject : String | Nil, @to : Array(String), @from : String | Nil, @bcc : Array(String) | Nil, @cc : Array(String) | Nil, @attachments : Array(String) | Nil, @created_at : Time, @read : Bool, @in_reply_to : String | Nil, @message_id : String | Nil, @thread_id : String | Nil, @sender : Sender | Nil, @recipients : EmailRecipients | Nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @item_type.nil?
        invalid_properties.push("invalid value for \"item_type\", item_type cannot be nil.")
      end

      if @entity_id.nil?
        invalid_properties.push("invalid value for \"entity_id\", entity_id cannot be nil.")
      end

      if @to.nil?
        invalid_properties.push("invalid value for \"to\", to cannot be nil.")
      end

      if @created_at.nil?
        invalid_properties.push("invalid value for \"created_at\", created_at cannot be nil.")
      end

      if @read.nil?
        invalid_properties.push("invalid value for \"read\", read cannot be nil.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @item_type.nil?
      item_type_validator = EnumAttributeValidator.new("String", ["RECEIVED_EMAIL", "SENT_EMAIL"])
      return false unless item_type_validator.valid?(@item_type)
      return false if @entity_id.nil?
      return false if @to.nil?
      return false if @created_at.nil?
      return false if @read.nil?
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] item_type Object to be assigned
    def item_type=(item_type)
      validator = EnumAttributeValidator.new("String", ["RECEIVED_EMAIL", "SENT_EMAIL"])
      unless validator.valid?(item_type)
        raise ArgumentError.new("invalid value for \"item_type\", must be one of #{validator.allowable_values}.")
      end
      @item_type = item_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          item_type == o.item_type &&
          entity_id == o.entity_id &&
          body_excerpt == o.body_excerpt &&
          text_excerpt == o.text_excerpt &&
          subject == o.subject &&
          to == o.to &&
          from == o.from &&
          bcc == o.bcc &&
          cc == o.cc &&
          attachments == o.attachments &&
          created_at == o.created_at &&
          read == o.read &&
          in_reply_to == o.in_reply_to &&
          message_id == o.message_id &&
          thread_id == o.thread_id &&
          sender == o.sender &&
          recipients == o.recipients
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [item_type, entity_id, body_excerpt, text_excerpt, subject, to, from, bcc, cc, attachments, created_at, read, in_reply_to, message_id, thread_id, sender, recipients].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
