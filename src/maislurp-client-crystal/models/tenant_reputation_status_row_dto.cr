# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  class TenantReputationStatusRowDto include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: accountRegion, type: String)]
    property account_region : String


    @[JSON::Field(key: tenantName, type: String)]
    property tenant_name : String


    @[JSON::Field(key: tenantArn, type: String)]
    property tenant_arn : String


    @[JSON::Field(key: sendingStatus, type: String)]
    property sending_status : String


    @[JSON::Field(key: reputationStatus, type: String)]
    property reputation_status : String


    @[JSON::Field(key: reputationPolicy, type: String)]
    property reputation_policy : String


    @[JSON::Field(key: customerManagedSendingStatus, type: String)]
    property customer_managed_sending_status : String


    @[JSON::Field(key: awsManagedSendingStatus, type: String)]
    property aws_managed_sending_status : String


    @[JSON::Field(key: findingCount, type: Int32)]
    property finding_count : Int32


    @[JSON::Field(key: bounceRate, type: Float64)]
    property bounce_rate : Float64


    @[JSON::Field(key: complaintRate, type: Float64)]
    property complaint_rate : Float64


    @[JSON::Field(key: sendLastHour, type: Float64)]
    property send_last_hour : Float64


    @[JSON::Field(key: metricTimestamp, type: Time)]
    property metric_timestamp : Time


    @[JSON::Field(key: error, type: String)]
    property error : String

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@account_region : String, @tenant_name : String, @tenant_arn : String | Nil, @sending_status : String | Nil, @reputation_status : String | Nil, @reputation_policy : String | Nil, @customer_managed_sending_status : String | Nil, @aws_managed_sending_status : String | Nil, @finding_count : Int32, @bounce_rate : Float64 | Nil, @complaint_rate : Float64 | Nil, @send_last_hour : Float64 | Nil, @metric_timestamp : Time | Nil, @error : String | Nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @account_region.nil?
        invalid_properties.push("invalid value for \"account_region\", account_region cannot be nil.")
      end

      if @tenant_name.nil?
        invalid_properties.push("invalid value for \"tenant_name\", tenant_name cannot be nil.")
      end

      if @finding_count.nil?
        invalid_properties.push("invalid value for \"finding_count\", finding_count cannot be nil.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @account_region.nil?
      account_region_validator = EnumAttributeValidator.new("String", ["US_WEST_2_ACCOUNT_STAGING", "US_EAST_1_ACCOUNT_STAGING", "EU_WEST_1_ACCOUNT_STAGING", "US_WEST_2_ACCOUNT_SES_1", "EU_WEST_1_ACCOUNT_SES_1", "US_WEST_2_ACCOUNT_SES_2", "EU_WEST_1_ACCOUNT_SES_2", "US_WEST_2_ACCOUNT_BYTEWISE", "EU_WEST_1_ACCOUNT_BYTEWISE", "US_WEST_2", "EU_WEST_1", "US_EAST_1"])
      return false unless account_region_validator.valid?(@account_region)
      return false if @tenant_name.nil?
      return false if @finding_count.nil?
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] account_region Object to be assigned
    def account_region=(account_region)
      validator = EnumAttributeValidator.new("String", ["US_WEST_2_ACCOUNT_STAGING", "US_EAST_1_ACCOUNT_STAGING", "EU_WEST_1_ACCOUNT_STAGING", "US_WEST_2_ACCOUNT_SES_1", "EU_WEST_1_ACCOUNT_SES_1", "US_WEST_2_ACCOUNT_SES_2", "EU_WEST_1_ACCOUNT_SES_2", "US_WEST_2_ACCOUNT_BYTEWISE", "EU_WEST_1_ACCOUNT_BYTEWISE", "US_WEST_2", "EU_WEST_1", "US_EAST_1"])
      unless validator.valid?(account_region)
        raise ArgumentError.new("invalid value for \"account_region\", must be one of #{validator.allowable_values}.")
      end
      @account_region = account_region
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          account_region == o.account_region &&
          tenant_name == o.tenant_name &&
          tenant_arn == o.tenant_arn &&
          sending_status == o.sending_status &&
          reputation_status == o.reputation_status &&
          reputation_policy == o.reputation_policy &&
          customer_managed_sending_status == o.customer_managed_sending_status &&
          aws_managed_sending_status == o.aws_managed_sending_status &&
          finding_count == o.finding_count &&
          bounce_rate == o.bounce_rate &&
          complaint_rate == o.complaint_rate &&
          send_last_hour == o.send_last_hour &&
          metric_timestamp == o.metric_timestamp &&
          error == o.error
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [account_region, tenant_name, tenant_arn, sending_status, reputation_status, reputation_policy, customer_managed_sending_status, aws_managed_sending_status, finding_count, bounce_rate, complaint_rate, send_last_hour, metric_timestamp, error].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
