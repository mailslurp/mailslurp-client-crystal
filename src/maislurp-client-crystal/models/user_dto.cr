# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails from dynamically allocated email addresses. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://www.mailslurp.com/docs/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  # Representation a user's account
  class UserDto include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: id, type: String)]
    property id : String


    @[JSON::Field(key: apiKey, type: String)]
    property api_key : String


    @[JSON::Field(key: emailAddress, type: String)]
    property email_address : String


    @[JSON::Field(key: emailAddressMd5, type: String)]
    property email_address_md5 : String


    @[JSON::Field(key: created, type: Time)]
    property created : Time


    @[JSON::Field(key: userType, type: String)]
    property user_type : String


    # Does user belong to an organization
    @[JSON::Field(key: organization, type: String)]
    property organization : String


    # Has user accepted an organization invite
    @[JSON::Field(key: verified, type: String)]
    property verified : String


    @[JSON::Field(key: hasPassword, type: Bool)]
    property has_password : Bool


    @[JSON::Field(key: isFrozen, type: Bool)]
    property is_frozen : Bool


    @[JSON::Field(key: addNewContacts, type: Bool)]
    property add_new_contacts : Bool


    @[JSON::Field(key: ssoProvider, type: String)]
    property sso_provider : String


    @[JSON::Field(key: customerId, type: String)]
    property customer_id : String


    @[JSON::Field(key: hasOnboarded, type: Bool)]
    property has_onboarded : Bool


    @[JSON::Field(key: imapUsername, type: String)]
    property imap_username : String


    @[JSON::Field(key: imapPassword, type: String)]
    property imap_password : String


    @[JSON::Field(key: smtpUsername, type: String)]
    property smtp_username : String


    @[JSON::Field(key: smtpPassword, type: String)]
    property smtp_password : String

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : String, @api_key : String, @email_address : String, @email_address_md5 : String, @created : Time | Nil, @user_type : String | Nil, @organization : String | Nil, @verified : String | Nil, @has_password : Bool, @is_frozen : Bool, @add_new_contacts : Bool | Nil, @sso_provider : String | Nil, @customer_id : String | Nil, @has_onboarded : Bool | Nil, @imap_username : String | Nil, @imap_password : String | Nil, @smtp_username : String | Nil, @smtp_password : String | Nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @id.nil?
        invalid_properties.push("invalid value for \"id\", id cannot be nil.")
      end

      if @api_key.nil?
        invalid_properties.push("invalid value for \"api_key\", api_key cannot be nil.")
      end

      if @email_address.nil?
        invalid_properties.push("invalid value for \"email_address\", email_address cannot be nil.")
      end

      if @email_address_md5.nil?
        invalid_properties.push("invalid value for \"email_address_md5\", email_address_md5 cannot be nil.")
      end

      if @has_password.nil?
        invalid_properties.push("invalid value for \"has_password\", has_password cannot be nil.")
      end

      if @is_frozen.nil?
        invalid_properties.push("invalid value for \"is_frozen\", is_frozen cannot be nil.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @id.nil?
      return false if @api_key.nil?
      return false if @email_address.nil?
      return false if @email_address_md5.nil?
      user_type_validator = EnumAttributeValidator.new("String", ["SOLO", "CHILD_SOLO", "CHILD_TEAM"])
      return false unless user_type_validator.valid?(@user_type)
      verified_validator = EnumAttributeValidator.new("String", ["VERIFIED", "PENDING"])
      return false unless verified_validator.valid?(@verified)
      return false if @has_password.nil?
      return false if @is_frozen.nil?
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] user_type Object to be assigned
    def user_type=(user_type)
      validator = EnumAttributeValidator.new("String", ["SOLO", "CHILD_SOLO", "CHILD_TEAM"])
      unless validator.valid?(user_type)
        raise ArgumentError.new("invalid value for \"user_type\", must be one of #{validator.allowable_values}.")
      end
      @user_type = user_type
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] verified Object to be assigned
    def verified=(verified)
      validator = EnumAttributeValidator.new("String", ["VERIFIED", "PENDING"])
      unless validator.valid?(verified)
        raise ArgumentError.new("invalid value for \"verified\", must be one of #{validator.allowable_values}.")
      end
      @verified = verified
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          id == o.id &&
          api_key == o.api_key &&
          email_address == o.email_address &&
          email_address_md5 == o.email_address_md5 &&
          created == o.created &&
          user_type == o.user_type &&
          organization == o.organization &&
          verified == o.verified &&
          has_password == o.has_password &&
          is_frozen == o.is_frozen &&
          add_new_contacts == o.add_new_contacts &&
          sso_provider == o.sso_provider &&
          customer_id == o.customer_id &&
          has_onboarded == o.has_onboarded &&
          imap_username == o.imap_username &&
          imap_password == o.imap_password &&
          smtp_username == o.smtp_username &&
          smtp_password == o.smtp_password
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, api_key, email_address, email_address_md5, created, user_type, organization, verified, has_password, is_frozen, add_new_contacts, sso_provider, customer_id, has_onboarded, imap_username, imap_password, smtp_username, smtp_password].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
