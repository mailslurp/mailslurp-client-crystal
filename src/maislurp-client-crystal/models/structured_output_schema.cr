# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  # JSON output schema for structured content repsonses. This schema dictates the format that an AI should use when responding to your instructions.
  class StructuredOutputSchema include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: anyOf, type: Array(StructuredOutputSchema), nilable: true, emit_null: true)]
    property any_of : Array(StructuredOutputSchema)


    @[JSON::Field(key: default, type: Object, nilable: true, emit_null: true)]
    property default : Object


    # Provide a description of the schema to help the AI understand the schema.
    @[JSON::Field(key: description, type: String, nilable: true, emit_null: true)]
    property description : String


    # When using type string and format enum pass a collection of enum values here.
    @[JSON::Field(key: enum, type: Array(String), nilable: true, emit_null: true)]
    property _enum : Array(String)


    @[JSON::Field(key: example, type: Object, nilable: true, emit_null: true)]
    property example : Object


    # Format for string types. Can be null, date-time or enum.
    @[JSON::Field(key: format, type: String, nilable: true, emit_null: true)]
    property format : String


    @[JSON::Field(key: items, type: StructuredOutputSchema)]
    property items : StructuredOutputSchema


    @[JSON::Field(key: maxItems, type: Int64, nilable: true, emit_null: true)]
    property max_items : Int64


    @[JSON::Field(key: minItems, type: Int64, nilable: true, emit_null: true)]
    property min_items : Int64


    @[JSON::Field(key: maxLength, type: Int64, nilable: true, emit_null: true)]
    property max_length : Int64


    @[JSON::Field(key: minLength, type: Int64, nilable: true, emit_null: true)]
    property min_length : Int64


    @[JSON::Field(key: pattern, type: String, nilable: true, emit_null: true)]
    property pattern : String


    # Properties of an OBJECT schema. These are key value pairs where the key is the property name and the value is the schema for that property.
    @[JSON::Field(key: properties, type: Hash(String, StructuredOutputSchema), nilable: true, emit_null: true)]
    property properties : Hash(String, StructuredOutputSchema)


    # Pass an array of property names to specify the order of properties in the generated JSON object if required.
    @[JSON::Field(key: propertyOrdering, type: Array(String), nilable: true, emit_null: true)]
    property property_ordering : Array(String)


    @[JSON::Field(key: required, type: Array(String), nilable: true, emit_null: true)]
    property required : Array(String)


    @[JSON::Field(key: maxProperties, type: Int64, nilable: true, emit_null: true)]
    property max_properties : Int64


    @[JSON::Field(key: minProperties, type: Int64, nilable: true, emit_null: true)]
    property min_properties : Int64


    @[JSON::Field(key: maximum, type: Float64, nilable: true, emit_null: true)]
    property maximum : Float64


    @[JSON::Field(key: minimum, type: Float64, nilable: true, emit_null: true)]
    property minimum : Float64


    @[JSON::Field(key: nullable, type: Bool, nilable: true, emit_null: true)]
    property nullable : Bool


    @[JSON::Field(key: title, type: String, nilable: true, emit_null: true)]
    property title : String


    # Primitive JSON schema types with a fallback CUSTOM for unknown values.
    @[JSON::Field(key: type, type: String, nilable: true, emit_null: true)]
    property _type : String

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@any_of : Array(StructuredOutputSchema) | Nil, @default : Object | Nil, @description : String | Nil, @_enum : Array(String) | Nil, @example : Object | Nil, @format : String | Nil, @items : StructuredOutputSchema | Nil, @max_items : Int64 | Nil, @min_items : Int64 | Nil, @max_length : Int64 | Nil, @min_length : Int64 | Nil, @pattern : String | Nil, @properties : Hash(String, StructuredOutputSchema) | Nil, @property_ordering : Array(String) | Nil, @required : Array(String) | Nil, @max_properties : Int64 | Nil, @min_properties : Int64 | Nil, @maximum : Float64 | Nil, @minimum : Float64 | Nil, @nullable : Bool | Nil, @title : String | Nil, @_type : String | Nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if !@max_items.nil? && @max_items < 0
        invalid_properties.push("invalid value for \"max_items\", must be greater than or equal to 0.")
      end

      if !@min_items.nil? && @min_items < 0
        invalid_properties.push("invalid value for \"min_items\", must be greater than or equal to 0.")
      end

      if !@max_length.nil? && @max_length < 0
        invalid_properties.push("invalid value for \"max_length\", must be greater than or equal to 0.")
      end

      if !@min_length.nil? && @min_length < 0
        invalid_properties.push("invalid value for \"min_length\", must be greater than or equal to 0.")
      end

      if !@max_properties.nil? && @max_properties < 0
        invalid_properties.push("invalid value for \"max_properties\", must be greater than or equal to 0.")
      end

      if !@min_properties.nil? && @min_properties < 0
        invalid_properties.push("invalid value for \"min_properties\", must be greater than or equal to 0.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if !@max_items.nil? && @max_items < 0
      return false if !@min_items.nil? && @min_items < 0
      return false if !@max_length.nil? && @max_length < 0
      return false if !@min_length.nil? && @min_length < 0
      return false if !@max_properties.nil? && @max_properties < 0
      return false if !@min_properties.nil? && @min_properties < 0
      _type_validator = EnumAttributeValidator.new("String", ["string", "number", "integer", "boolean", "object", "array", "null"])
      return false unless _type_validator.valid?(@_type)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] max_items Value to be assigned
    def max_items=(max_items)
      if !max_items.nil? && max_items < 0
        raise ArgumentError.new("invalid value for \"max_items\", must be greater than or equal to 0.")
      end

      @max_items = max_items
    end

    # Custom attribute writer method with validation
    # @param [Object] min_items Value to be assigned
    def min_items=(min_items)
      if !min_items.nil? && min_items < 0
        raise ArgumentError.new("invalid value for \"min_items\", must be greater than or equal to 0.")
      end

      @min_items = min_items
    end

    # Custom attribute writer method with validation
    # @param [Object] max_length Value to be assigned
    def max_length=(max_length)
      if !max_length.nil? && max_length < 0
        raise ArgumentError.new("invalid value for \"max_length\", must be greater than or equal to 0.")
      end

      @max_length = max_length
    end

    # Custom attribute writer method with validation
    # @param [Object] min_length Value to be assigned
    def min_length=(min_length)
      if !min_length.nil? && min_length < 0
        raise ArgumentError.new("invalid value for \"min_length\", must be greater than or equal to 0.")
      end

      @min_length = min_length
    end

    # Custom attribute writer method with validation
    # @param [Object] max_properties Value to be assigned
    def max_properties=(max_properties)
      if !max_properties.nil? && max_properties < 0
        raise ArgumentError.new("invalid value for \"max_properties\", must be greater than or equal to 0.")
      end

      @max_properties = max_properties
    end

    # Custom attribute writer method with validation
    # @param [Object] min_properties Value to be assigned
    def min_properties=(min_properties)
      if !min_properties.nil? && min_properties < 0
        raise ArgumentError.new("invalid value for \"min_properties\", must be greater than or equal to 0.")
      end

      @min_properties = min_properties
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] _type Object to be assigned
    def _type=(_type)
      validator = EnumAttributeValidator.new("String", ["string", "number", "integer", "boolean", "object", "array", "null"])
      unless validator.valid?(_type)
        raise ArgumentError.new("invalid value for \"_type\", must be one of #{validator.allowable_values}.")
      end
      @_type = _type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          any_of == o.any_of &&
          default == o.default &&
          description == o.description &&
          _enum == o._enum &&
          example == o.example &&
          format == o.format &&
          items == o.items &&
          max_items == o.max_items &&
          min_items == o.min_items &&
          max_length == o.max_length &&
          min_length == o.min_length &&
          pattern == o.pattern &&
          properties == o.properties &&
          property_ordering == o.property_ordering &&
          required == o.required &&
          max_properties == o.max_properties &&
          min_properties == o.min_properties &&
          maximum == o.maximum &&
          minimum == o.minimum &&
          nullable == o.nullable &&
          title == o.title &&
          _type == o._type
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [any_of, default, description, _enum, example, format, items, max_items, min_items, max_length, min_length, pattern, properties, property_ordering, required, max_properties, min_properties, maximum, minimum, nullable, title, _type].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
