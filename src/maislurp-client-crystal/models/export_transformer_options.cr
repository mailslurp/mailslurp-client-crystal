# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  class ExportTransformerOptions include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: aiTransformId, type: String, nilable: true, emit_null: true)]
    property ai_transform_id : String


    @[JSON::Field(key: aiMappingId, type: String, nilable: true, emit_null: true)]
    property ai_mapping_id : String


    @[JSON::Field(key: flattenArrays, type: Bool)]
    property flatten_arrays : Bool


    @[JSON::Field(key: since, type: Time, nilable: true, emit_null: true)]
    property since : Time


    @[JSON::Field(key: before, type: Time, nilable: true, emit_null: true)]
    property before : Time


    @[JSON::Field(key: format, type: String)]
    property format : String


    @[JSON::Field(key: inboxId, type: String, nilable: true, emit_null: true)]
    property inbox_id : String


    @[JSON::Field(key: phoneId, type: String, nilable: true, emit_null: true)]
    property phone_id : String


    @[JSON::Field(key: emailId, type: String, nilable: true, emit_null: true)]
    property email_id : String


    @[JSON::Field(key: smsId, type: String, nilable: true, emit_null: true)]
    property sms_id : String


    @[JSON::Field(key: attachmentId, type: String, nilable: true, emit_null: true)]
    property attachment_id : String


    @[JSON::Field(key: includeMetaData, type: Bool)]
    property include_meta_data : Bool


    @[JSON::Field(key: delimiter, type: String, nilable: true, emit_null: true)]
    property delimiter : String


    @[JSON::Field(key: arraySeparator, type: String, nilable: true, emit_null: true)]
    property array_separator : String


    @[JSON::Field(key: includeMetaDataColumns, type: Array(String), nilable: true, emit_null: true)]
    property include_meta_data_columns : Array(String)

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@ai_transform_id : String | Nil, @ai_mapping_id : String | Nil, @flatten_arrays : Bool, @since : Time | Nil, @before : Time | Nil, @format : String, @inbox_id : String | Nil, @phone_id : String | Nil, @email_id : String | Nil, @sms_id : String | Nil, @attachment_id : String | Nil, @include_meta_data : Bool, @delimiter : String | Nil, @array_separator : String | Nil, @include_meta_data_columns : Array(String) | Nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @flatten_arrays.nil?
        invalid_properties.push("invalid value for \"flatten_arrays\", flatten_arrays cannot be nil.")
      end

      if @format.nil?
        invalid_properties.push("invalid value for \"format\", format cannot be nil.")
      end

      if @include_meta_data.nil?
        invalid_properties.push("invalid value for \"include_meta_data\", include_meta_data cannot be nil.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @flatten_arrays.nil?
      return false if @format.nil?
      format_validator = EnumAttributeValidator.new("String", ["CSV", "EXCEL", "XML", "JSON"])
      return false unless format_validator.valid?(@format)
      return false if @include_meta_data.nil?
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] format Object to be assigned
    def format=(format)
      validator = EnumAttributeValidator.new("String", ["CSV", "EXCEL", "XML", "JSON"])
      unless validator.valid?(format)
        raise ArgumentError.new("invalid value for \"format\", must be one of #{validator.allowable_values}.")
      end
      @format = format
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          ai_transform_id == o.ai_transform_id &&
          ai_mapping_id == o.ai_mapping_id &&
          flatten_arrays == o.flatten_arrays &&
          since == o.since &&
          before == o.before &&
          format == o.format &&
          inbox_id == o.inbox_id &&
          phone_id == o.phone_id &&
          email_id == o.email_id &&
          sms_id == o.sms_id &&
          attachment_id == o.attachment_id &&
          include_meta_data == o.include_meta_data &&
          delimiter == o.delimiter &&
          array_separator == o.array_separator &&
          include_meta_data_columns == o.include_meta_data_columns
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [ai_transform_id, ai_mapping_id, flatten_arrays, since, before, format, inbox_id, phone_id, email_id, sms_id, attachment_id, include_meta_data, delimiter, array_separator, include_meta_data_columns].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
