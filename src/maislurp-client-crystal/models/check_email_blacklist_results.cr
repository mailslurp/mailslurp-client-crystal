# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  # Public blacklist lookup results for a domain or IP address
  class CheckEmailBlacklistResults include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: domain, type: String, nilable: true, emit_null: true)]
    property domain : String


    @[JSON::Field(key: requestedIpAddress, type: String, nilable: true, emit_null: true)]
    property requested_ip_address : String


    @[JSON::Field(key: requestedMxHost, type: String, nilable: true, emit_null: true)]
    property requested_mx_host : String


    @[JSON::Field(key: status, type: String)]
    property status : String


    @[JSON::Field(key: listed, type: Bool)]
    property listed : Bool


    @[JSON::Field(key: checkedIpAddresses, type: Array(EmailBlacklistIpResult))]
    property checked_ip_addresses : Array(EmailBlacklistIpResult)


    @[JSON::Field(key: checkedZoneCount, type: Int32)]
    property checked_zone_count : Int32


    @[JSON::Field(key: totalListings, type: Int32)]
    property total_listings : Int32


    @[JSON::Field(key: warnings, type: Array(String))]
    property warnings : Array(String)


    @[JSON::Field(key: errors, type: Array(String))]
    property errors : Array(String)

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@domain : String | Nil, @requested_ip_address : String | Nil, @requested_mx_host : String | Nil, @status : String, @listed : Bool, @checked_ip_addresses : Array(EmailBlacklistIpResult), @checked_zone_count : Int32, @total_listings : Int32, @warnings : Array(String), @errors : Array(String))
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @status.nil?
        invalid_properties.push("invalid value for \"status\", status cannot be nil.")
      end

      if @listed.nil?
        invalid_properties.push("invalid value for \"listed\", listed cannot be nil.")
      end

      if @checked_ip_addresses.nil?
        invalid_properties.push("invalid value for \"checked_ip_addresses\", checked_ip_addresses cannot be nil.")
      end

      if @checked_zone_count.nil?
        invalid_properties.push("invalid value for \"checked_zone_count\", checked_zone_count cannot be nil.")
      end

      if @total_listings.nil?
        invalid_properties.push("invalid value for \"total_listings\", total_listings cannot be nil.")
      end

      if @warnings.nil?
        invalid_properties.push("invalid value for \"warnings\", warnings cannot be nil.")
      end

      if @errors.nil?
        invalid_properties.push("invalid value for \"errors\", errors cannot be nil.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @status.nil?
      status_validator = EnumAttributeValidator.new("String", ["HEALTHY", "DEGRADED", "CRITICAL", "FAILED"])
      return false unless status_validator.valid?(@status)
      return false if @listed.nil?
      return false if @checked_ip_addresses.nil?
      return false if @checked_zone_count.nil?
      return false if @total_listings.nil?
      return false if @warnings.nil?
      return false if @errors.nil?
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidator.new("String", ["HEALTHY", "DEGRADED", "CRITICAL", "FAILED"])
      unless validator.valid?(status)
        raise ArgumentError.new("invalid value for \"status\", must be one of #{validator.allowable_values}.")
      end
      @status = status
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          domain == o.domain &&
          requested_ip_address == o.requested_ip_address &&
          requested_mx_host == o.requested_mx_host &&
          status == o.status &&
          listed == o.listed &&
          checked_ip_addresses == o.checked_ip_addresses &&
          checked_zone_count == o.checked_zone_count &&
          total_listings == o.total_listings &&
          warnings == o.warnings &&
          errors == o.errors
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [domain, requested_ip_address, requested_mx_host, status, listed, checked_ip_addresses, checked_zone_count, total_listings, warnings, errors].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
