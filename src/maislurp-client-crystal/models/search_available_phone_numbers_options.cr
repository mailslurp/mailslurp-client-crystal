# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  # Search criteria for advanced phone provisioning
  class SearchAvailablePhoneNumbersOptions include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: phoneCountry, type: String)]
    property phone_country : String


    @[JSON::Field(key: phoneVariant, type: String, nilable: true, emit_null: true)]
    property phone_variant : String


    # Quality filter for advanced phone provisioning search
    @[JSON::Field(key: qualityFilter, type: String, nilable: true, emit_null: true)]
    property quality_filter : String


    @[JSON::Field(key: lineType, type: String, nilable: true, emit_null: true)]
    property line_type : String


    @[JSON::Field(key: carrierName, type: String, nilable: true, emit_null: true)]
    property carrier_name : String


    @[JSON::Field(key: mobileCountryCode, type: String, nilable: true, emit_null: true)]
    property mobile_country_code : String


    @[JSON::Field(key: mobileNetworkCode, type: String, nilable: true, emit_null: true)]
    property mobile_network_code : String


    @[JSON::Field(key: providerLabels, type: Array(String), nilable: true, emit_null: true)]
    property provider_labels : Array(String)


    @[JSON::Field(key: limit, type: Int32, nilable: true, emit_null: true)]
    property limit : Int32

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@phone_country : String, @phone_variant : String | Nil, @quality_filter : String | Nil, @line_type : String | Nil, @carrier_name : String | Nil, @mobile_country_code : String | Nil, @mobile_network_code : String | Nil, @provider_labels : Array(String) | Nil, @limit : Int32 | Nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @phone_country.nil?
        invalid_properties.push("invalid value for \"phone_country\", phone_country cannot be nil.")
      end

      if !@limit.nil? && @limit > 100
        invalid_properties.push("invalid value for \"limit\", must be smaller than or equal to 100.")
      end

      if !@limit.nil? && @limit < 1
        invalid_properties.push("invalid value for \"limit\", must be greater than or equal to 1.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @phone_country.nil?
      phone_country_validator = EnumAttributeValidator.new("String", ["US", "GB", "AU", "CA", "EE", "HK", "PL", "PT", "NL", "IL", "FI", "SE"])
      return false unless phone_country_validator.valid?(@phone_country)
      phone_variant_validator = EnumAttributeValidator.new("String", ["LOCAL", "MOBILE", "TOLL_FREE"])
      return false unless phone_variant_validator.valid?(@phone_variant)
      quality_filter_validator = EnumAttributeValidator.new("String", ["OTP", "NON_VOIP", "MOBILE_ONLY", "ANY"])
      return false unless quality_filter_validator.valid?(@quality_filter)
      return false if !@limit.nil? && @limit > 100
      return false if !@limit.nil? && @limit < 1
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] phone_country Object to be assigned
    def phone_country=(phone_country)
      validator = EnumAttributeValidator.new("String", ["US", "GB", "AU", "CA", "EE", "HK", "PL", "PT", "NL", "IL", "FI", "SE"])
      unless validator.valid?(phone_country)
        raise ArgumentError.new("invalid value for \"phone_country\", must be one of #{validator.allowable_values}.")
      end
      @phone_country = phone_country
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] phone_variant Object to be assigned
    def phone_variant=(phone_variant)
      validator = EnumAttributeValidator.new("String", ["LOCAL", "MOBILE", "TOLL_FREE"])
      unless validator.valid?(phone_variant)
        raise ArgumentError.new("invalid value for \"phone_variant\", must be one of #{validator.allowable_values}.")
      end
      @phone_variant = phone_variant
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] quality_filter Object to be assigned
    def quality_filter=(quality_filter)
      validator = EnumAttributeValidator.new("String", ["OTP", "NON_VOIP", "MOBILE_ONLY", "ANY"])
      unless validator.valid?(quality_filter)
        raise ArgumentError.new("invalid value for \"quality_filter\", must be one of #{validator.allowable_values}.")
      end
      @quality_filter = quality_filter
    end

    # Custom attribute writer method with validation
    # @param [Object] limit Value to be assigned
    def limit=(limit)
      if !limit.nil? && limit > 100
        raise ArgumentError.new("invalid value for \"limit\", must be smaller than or equal to 100.")
      end

      if !limit.nil? && limit < 1
        raise ArgumentError.new("invalid value for \"limit\", must be greater than or equal to 1.")
      end

      @limit = limit
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          phone_country == o.phone_country &&
          phone_variant == o.phone_variant &&
          quality_filter == o.quality_filter &&
          line_type == o.line_type &&
          carrier_name == o.carrier_name &&
          mobile_country_code == o.mobile_country_code &&
          mobile_network_code == o.mobile_network_code &&
          provider_labels == o.provider_labels &&
          limit == o.limit
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [phone_country, phone_variant, quality_filter, line_type, carrier_name, mobile_country_code, mobile_network_code, provider_labels, limit].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
