# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  class CampaignProbeRunComparisonDto include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: baseline, type: CampaignProbeRunDto)]
    property baseline : CampaignProbeRunDto


    @[JSON::Field(key: current, type: CampaignProbeRunDto)]
    property current : CampaignProbeRunDto


    @[JSON::Field(key: healthScoreDelta, type: Int32)]
    property health_score_delta : Int32


    @[JSON::Field(key: statusChanged, type: Bool)]
    property status_changed : Bool


    @[JSON::Field(key: linkIssueDelta, type: Int32)]
    property link_issue_delta : Int32


    @[JSON::Field(key: imageIssueDelta, type: Int32)]
    property image_issue_delta : Int32


    @[JSON::Field(key: compatibilityWarningDelta, type: Int32)]
    property compatibility_warning_delta : Int32


    @[JSON::Field(key: compatibilityNotSupportedDelta, type: Int32)]
    property compatibility_not_supported_delta : Int32


    @[JSON::Field(key: htmlErrorDelta, type: Int32)]
    property html_error_delta : Int32


    @[JSON::Field(key: htmlWarningDelta, type: Int32)]
    property html_warning_delta : Int32


    @[JSON::Field(key: reputationFailureDelta, type: Int32)]
    property reputation_failure_delta : Int32


    @[JSON::Field(key: attachmentMentionIssueDelta, type: Int32)]
    property attachment_mention_issue_delta : Int32


    @[JSON::Field(key: addedInsights, type: Array(String))]
    property added_insights : Array(String)


    @[JSON::Field(key: removedInsights, type: Array(String))]
    property removed_insights : Array(String)

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@baseline : CampaignProbeRunDto, @current : CampaignProbeRunDto, @health_score_delta : Int32, @status_changed : Bool, @link_issue_delta : Int32, @image_issue_delta : Int32, @compatibility_warning_delta : Int32, @compatibility_not_supported_delta : Int32, @html_error_delta : Int32, @html_warning_delta : Int32, @reputation_failure_delta : Int32, @attachment_mention_issue_delta : Int32, @added_insights : Array(String), @removed_insights : Array(String))
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @baseline.nil?
        invalid_properties.push("invalid value for \"baseline\", baseline cannot be nil.")
      end

      if @current.nil?
        invalid_properties.push("invalid value for \"current\", current cannot be nil.")
      end

      if @health_score_delta.nil?
        invalid_properties.push("invalid value for \"health_score_delta\", health_score_delta cannot be nil.")
      end

      if @status_changed.nil?
        invalid_properties.push("invalid value for \"status_changed\", status_changed cannot be nil.")
      end

      if @link_issue_delta.nil?
        invalid_properties.push("invalid value for \"link_issue_delta\", link_issue_delta cannot be nil.")
      end

      if @image_issue_delta.nil?
        invalid_properties.push("invalid value for \"image_issue_delta\", image_issue_delta cannot be nil.")
      end

      if @compatibility_warning_delta.nil?
        invalid_properties.push("invalid value for \"compatibility_warning_delta\", compatibility_warning_delta cannot be nil.")
      end

      if @compatibility_not_supported_delta.nil?
        invalid_properties.push("invalid value for \"compatibility_not_supported_delta\", compatibility_not_supported_delta cannot be nil.")
      end

      if @html_error_delta.nil?
        invalid_properties.push("invalid value for \"html_error_delta\", html_error_delta cannot be nil.")
      end

      if @html_warning_delta.nil?
        invalid_properties.push("invalid value for \"html_warning_delta\", html_warning_delta cannot be nil.")
      end

      if @reputation_failure_delta.nil?
        invalid_properties.push("invalid value for \"reputation_failure_delta\", reputation_failure_delta cannot be nil.")
      end

      if @attachment_mention_issue_delta.nil?
        invalid_properties.push("invalid value for \"attachment_mention_issue_delta\", attachment_mention_issue_delta cannot be nil.")
      end

      if @added_insights.nil?
        invalid_properties.push("invalid value for \"added_insights\", added_insights cannot be nil.")
      end

      if @removed_insights.nil?
        invalid_properties.push("invalid value for \"removed_insights\", removed_insights cannot be nil.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @baseline.nil?
      return false if @current.nil?
      return false if @health_score_delta.nil?
      return false if @status_changed.nil?
      return false if @link_issue_delta.nil?
      return false if @image_issue_delta.nil?
      return false if @compatibility_warning_delta.nil?
      return false if @compatibility_not_supported_delta.nil?
      return false if @html_error_delta.nil?
      return false if @html_warning_delta.nil?
      return false if @reputation_failure_delta.nil?
      return false if @attachment_mention_issue_delta.nil?
      return false if @added_insights.nil?
      return false if @removed_insights.nil?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          baseline == o.baseline &&
          current == o.current &&
          health_score_delta == o.health_score_delta &&
          status_changed == o.status_changed &&
          link_issue_delta == o.link_issue_delta &&
          image_issue_delta == o.image_issue_delta &&
          compatibility_warning_delta == o.compatibility_warning_delta &&
          compatibility_not_supported_delta == o.compatibility_not_supported_delta &&
          html_error_delta == o.html_error_delta &&
          html_warning_delta == o.html_warning_delta &&
          reputation_failure_delta == o.reputation_failure_delta &&
          attachment_mention_issue_delta == o.attachment_mention_issue_delta &&
          added_insights == o.added_insights &&
          removed_insights == o.removed_insights
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [baseline, current, health_score_delta, status_changed, link_issue_delta, image_issue_delta, compatibility_warning_delta, compatibility_not_supported_delta, html_error_delta, html_warning_delta, reputation_failure_delta, attachment_mention_issue_delta, added_insights, removed_insights].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
