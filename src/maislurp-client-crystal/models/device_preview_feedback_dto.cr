# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  class DevicePreviewFeedbackDto include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: feedbackId, type: String)]
    property feedback_id : String


    @[JSON::Field(key: userId, type: String)]
    property user_id : String


    @[JSON::Field(key: source, type: String)]
    property source : String


    @[JSON::Field(key: category, type: String)]
    property category : String


    @[JSON::Field(key: status, type: String)]
    property status : String


    @[JSON::Field(key: rating, type: Int32)]
    property rating : Int32


    @[JSON::Field(key: runId, type: String)]
    property run_id : String


    @[JSON::Field(key: targetId, type: String)]
    property target_id : String


    @[JSON::Field(key: screenshotId, type: String)]
    property screenshot_id : String


    @[JSON::Field(key: provider, type: String)]
    property provider : String


    @[JSON::Field(key: title, type: String)]
    property title : String


    @[JSON::Field(key: comment, type: String)]
    property comment : String


    @[JSON::Field(key: internalNote, type: String)]
    property internal_note : String


    @[JSON::Field(key: sessionId, type: String)]
    property session_id : String


    @[JSON::Field(key: liveViewUrl, type: String)]
    property live_view_url : String


    @[JSON::Field(key: metadata, type: Hash(String, String))]
    property metadata : Hash(String, String)


    @[JSON::Field(key: createdAt, type: Time)]
    property created_at : Time


    @[JSON::Field(key: updatedAt, type: Time)]
    property updated_at : Time

    class EnumAttributeValidator
      getter datatype : String
      getter allowable_values : Array(String)

      def initialize(datatype, allowable_values)
        @datatype = datatype
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.includes?(value)
      end
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@feedback_id : String, @user_id : String, @source : String, @category : String, @status : String, @rating : Int32 | Nil, @run_id : String | Nil, @target_id : String | Nil, @screenshot_id : String | Nil, @provider : String | Nil, @title : String | Nil, @comment : String | Nil, @internal_note : String | Nil, @session_id : String | Nil, @live_view_url : String | Nil, @metadata : Hash(String, String) | Nil, @created_at : Time | Nil, @updated_at : Time | Nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @feedback_id.nil?
        invalid_properties.push("invalid value for \"feedback_id\", feedback_id cannot be nil.")
      end

      if @user_id.nil?
        invalid_properties.push("invalid value for \"user_id\", user_id cannot be nil.")
      end

      if @source.nil?
        invalid_properties.push("invalid value for \"source\", source cannot be nil.")
      end

      if @category.nil?
        invalid_properties.push("invalid value for \"category\", category cannot be nil.")
      end

      if @status.nil?
        invalid_properties.push("invalid value for \"status\", status cannot be nil.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @feedback_id.nil?
      return false if @user_id.nil?
      return false if @source.nil?
      source_validator = EnumAttributeValidator.new("String", ["API_SERVICE", "LAB_USER", "END_USER", "SYSTEM"])
      return false unless source_validator.valid?(@source)
      return false if @category.nil?
      category_validator = EnumAttributeValidator.new("String", ["GENERAL", "RUN_QUALITY", "SCREENSHOT_QUALITY", "PROVIDER_ISSUE", "BUG_REPORT", "FEATURE_REQUEST", "NOTE"])
      return false unless category_validator.valid?(@category)
      return false if @status.nil?
      status_validator = EnumAttributeValidator.new("String", ["OPEN", "ACKNOWLEDGED", "RESOLVED", "DISMISSED"])
      return false unless status_validator.valid?(@status)
      provider_validator = EnumAttributeValidator.new("String", ["GMAIL", "OUTLOOK", "YAHOO"])
      return false unless provider_validator.valid?(@provider)
      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] source Object to be assigned
    def source=(source)
      validator = EnumAttributeValidator.new("String", ["API_SERVICE", "LAB_USER", "END_USER", "SYSTEM"])
      unless validator.valid?(source)
        raise ArgumentError.new("invalid value for \"source\", must be one of #{validator.allowable_values}.")
      end
      @source = source
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] category Object to be assigned
    def category=(category)
      validator = EnumAttributeValidator.new("String", ["GENERAL", "RUN_QUALITY", "SCREENSHOT_QUALITY", "PROVIDER_ISSUE", "BUG_REPORT", "FEATURE_REQUEST", "NOTE"])
      unless validator.valid?(category)
        raise ArgumentError.new("invalid value for \"category\", must be one of #{validator.allowable_values}.")
      end
      @category = category
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] status Object to be assigned
    def status=(status)
      validator = EnumAttributeValidator.new("String", ["OPEN", "ACKNOWLEDGED", "RESOLVED", "DISMISSED"])
      unless validator.valid?(status)
        raise ArgumentError.new("invalid value for \"status\", must be one of #{validator.allowable_values}.")
      end
      @status = status
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] provider Object to be assigned
    def provider=(provider)
      validator = EnumAttributeValidator.new("String", ["GMAIL", "OUTLOOK", "YAHOO"])
      unless validator.valid?(provider)
        raise ArgumentError.new("invalid value for \"provider\", must be one of #{validator.allowable_values}.")
      end
      @provider = provider
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          feedback_id == o.feedback_id &&
          user_id == o.user_id &&
          source == o.source &&
          category == o.category &&
          status == o.status &&
          rating == o.rating &&
          run_id == o.run_id &&
          target_id == o.target_id &&
          screenshot_id == o.screenshot_id &&
          provider == o.provider &&
          title == o.title &&
          comment == o.comment &&
          internal_note == o.internal_note &&
          session_id == o.session_id &&
          live_view_url == o.live_view_url &&
          metadata == o.metadata &&
          created_at == o.created_at &&
          updated_at == o.updated_at
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [feedback_id, user_id, source, category, status, rating, run_id, target_id, screenshot_id, provider, title, comment, internal_note, session_id, live_view_url, metadata, created_at, updated_at].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
