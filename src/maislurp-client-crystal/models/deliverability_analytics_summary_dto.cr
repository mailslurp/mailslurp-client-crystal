# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "time"

module 
  # Aggregated deliverability metrics for a given set of runs
  class DeliverabilityAnalyticsSummaryDto include JSON::Serializable
   include JSON::Serializable 
    @[JSON::Field(key: totalRuns, type: Int64)]
    property total_runs : Int64


    @[JSON::Field(key: completeRuns, type: Int64)]
    property complete_runs : Int64


    @[JSON::Field(key: failedRuns, type: Int64)]
    property failed_runs : Int64


    @[JSON::Field(key: stoppedRuns, type: Int64)]
    property stopped_runs : Int64


    @[JSON::Field(key: runningRuns, type: Int64)]
    property running_runs : Int64


    @[JSON::Field(key: scheduledRuns, type: Int64)]
    property scheduled_runs : Int64


    @[JSON::Field(key: pausedRuns, type: Int64)]
    property paused_runs : Int64


    @[JSON::Field(key: createdRuns, type: Int64)]
    property created_runs : Int64


    @[JSON::Field(key: thresholdConfiguredRuns, type: Int64)]
    property threshold_configured_runs : Int64


    @[JSON::Field(key: thresholdMetRuns, type: Int64)]
    property threshold_met_runs : Int64


    @[JSON::Field(key: averageCompletionPercentage, type: Float64)]
    property average_completion_percentage : Float64


    @[JSON::Field(key: averageMatchedEntities, type: Float64)]
    property average_matched_entities : Float64


    @[JSON::Field(key: averageUnmatchedEntities, type: Float64)]
    property average_unmatched_entities : Float64

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@total_runs : Int64, @complete_runs : Int64, @failed_runs : Int64, @stopped_runs : Int64, @running_runs : Int64, @scheduled_runs : Int64, @paused_runs : Int64, @created_runs : Int64, @threshold_configured_runs : Int64, @threshold_met_runs : Int64, @average_completion_percentage : Float64, @average_matched_entities : Float64, @average_unmatched_entities : Float64)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      if @total_runs.nil?
        invalid_properties.push("invalid value for \"total_runs\", total_runs cannot be nil.")
      end

      if @complete_runs.nil?
        invalid_properties.push("invalid value for \"complete_runs\", complete_runs cannot be nil.")
      end

      if @failed_runs.nil?
        invalid_properties.push("invalid value for \"failed_runs\", failed_runs cannot be nil.")
      end

      if @stopped_runs.nil?
        invalid_properties.push("invalid value for \"stopped_runs\", stopped_runs cannot be nil.")
      end

      if @running_runs.nil?
        invalid_properties.push("invalid value for \"running_runs\", running_runs cannot be nil.")
      end

      if @scheduled_runs.nil?
        invalid_properties.push("invalid value for \"scheduled_runs\", scheduled_runs cannot be nil.")
      end

      if @paused_runs.nil?
        invalid_properties.push("invalid value for \"paused_runs\", paused_runs cannot be nil.")
      end

      if @created_runs.nil?
        invalid_properties.push("invalid value for \"created_runs\", created_runs cannot be nil.")
      end

      if @threshold_configured_runs.nil?
        invalid_properties.push("invalid value for \"threshold_configured_runs\", threshold_configured_runs cannot be nil.")
      end

      if @threshold_met_runs.nil?
        invalid_properties.push("invalid value for \"threshold_met_runs\", threshold_met_runs cannot be nil.")
      end

      if @average_completion_percentage.nil?
        invalid_properties.push("invalid value for \"average_completion_percentage\", average_completion_percentage cannot be nil.")
      end

      if @average_matched_entities.nil?
        invalid_properties.push("invalid value for \"average_matched_entities\", average_matched_entities cannot be nil.")
      end

      if @average_unmatched_entities.nil?
        invalid_properties.push("invalid value for \"average_unmatched_entities\", average_unmatched_entities cannot be nil.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @total_runs.nil?
      return false if @complete_runs.nil?
      return false if @failed_runs.nil?
      return false if @stopped_runs.nil?
      return false if @running_runs.nil?
      return false if @scheduled_runs.nil?
      return false if @paused_runs.nil?
      return false if @created_runs.nil?
      return false if @threshold_configured_runs.nil?
      return false if @threshold_met_runs.nil?
      return false if @average_completion_percentage.nil?
      return false if @average_matched_entities.nil?
      return false if @average_unmatched_entities.nil?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          total_runs == o.total_runs &&
          complete_runs == o.complete_runs &&
          failed_runs == o.failed_runs &&
          stopped_runs == o.stopped_runs &&
          running_runs == o.running_runs &&
          scheduled_runs == o.scheduled_runs &&
          paused_runs == o.paused_runs &&
          created_runs == o.created_runs &&
          threshold_configured_runs == o.threshold_configured_runs &&
          threshold_met_runs == o.threshold_met_runs &&
          average_completion_percentage == o.average_completion_percentage &&
          average_matched_entities == o.average_matched_entities &&
          average_unmatched_entities == o.average_unmatched_entities
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [total_runs, complete_runs, failed_runs, stopped_runs, running_runs, scheduled_runs, paused_runs, created_runs, threshold_configured_runs, threshold_met_runs, average_completion_percentage, average_matched_entities, average_unmatched_entities].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = .const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {} of Symbol => String
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.includes?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        ({} of Symbol => String).tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
