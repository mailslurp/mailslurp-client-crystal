# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "uri"

module 
  class EmailControllerApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Set IMAP flags associated with a message. Only supports '\\Seen' flag.
    # Applies RFC3501 IMAP flag operations on a message. Current implementation supports read/unread semantics via the `\\\\Seen` flag only.
    # @param email_id [String] 
    # @param imap_flag_operation_options [ImapFlagOperationOptions] 
    # @return [EmailPreview]
    def apply_imap_flag_operation(email_id : String, imap_flag_operation_options : ImapFlagOperationOptions)
      data, _status_code, _headers = apply_imap_flag_operation_with_http_info(email_id, imap_flag_operation_options)
      data
    end

    # Set IMAP flags associated with a message. Only supports &#39;\\Seen&#39; flag.
    # Applies RFC3501 IMAP flag operations on a message. Current implementation supports read/unread semantics via the &#x60;\\\\Seen&#x60; flag only.
    # @param email_id [String] 
    # @param imap_flag_operation_options [ImapFlagOperationOptions] 
    # @return [Array<(EmailPreview, Integer, Hash)>] EmailPreview data, response status code and response headers
    def apply_imap_flag_operation_with_http_info(email_id : String, imap_flag_operation_options : ImapFlagOperationOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.apply_imap_flag_operation ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.apply_imap_flag_operation")
      end
      # verify the required parameter "imap_flag_operation_options" is set
      if @api_client.config.client_side_validation && imap_flag_operation_options.nil?
        raise ArgumentError.new("Missing the required parameter 'imap_flag_operation_options' when calling EmailControllerApi.apply_imap_flag_operation")
      end
      # resource path
      local_var_path = "/emails/{emailId}/imap-flag-operation".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = imap_flag_operation_options.to_json

      # return_type
      return_type = "EmailPreview"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.apply_imap_flag_operation",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#apply_imap_flag_operation\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailPreview.from_json(data), status_code, headers
    end

    # Check whether an email send would be accepted
    # Validates sender/inbox context and recipient eligibility before attempting a send. Useful for preflight checks in UI or test workflows.
    # @param inbox_id [String] ID of the inbox you want to send the email from
    # @param send_email_options [SendEmailOptions] 
    # @return [CanSendEmailResults]
    def can_send(inbox_id : String, send_email_options : SendEmailOptions)
      data, _status_code, _headers = can_send_with_http_info(inbox_id, send_email_options)
      data
    end

    # Check whether an email send would be accepted
    # Validates sender/inbox context and recipient eligibility before attempting a send. Useful for preflight checks in UI or test workflows.
    # @param inbox_id [String] ID of the inbox you want to send the email from
    # @param send_email_options [SendEmailOptions] 
    # @return [Array<(CanSendEmailResults, Integer, Hash)>] CanSendEmailResults data, response status code and response headers
    def can_send_with_http_info(inbox_id : String, send_email_options : SendEmailOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.can_send ..."}
      end
      # verify the required parameter "inbox_id" is set
      if @api_client.config.client_side_validation && inbox_id.nil?
        raise ArgumentError.new("Missing the required parameter 'inbox_id' when calling EmailControllerApi.can_send")
      end
      # verify the required parameter "send_email_options" is set
      if @api_client.config.client_side_validation && send_email_options.nil?
        raise ArgumentError.new("Missing the required parameter 'send_email_options' when calling EmailControllerApi.can_send")
      end
      # resource path
      local_var_path = "/emails/can-send"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxId"] = inbox_id

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = send_email_options.to_json

      # return_type
      return_type = "CanSendEmailResults"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.can_send",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#can_send\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return CanSendEmailResults.from_json(data), status_code, headers
    end

    # Run aggregate email audit for a stored email
    # Runs the same message-level audit bundle used by the email audit dashboard in one request. Combines content checks, HTML validation, compatibility analysis, and reputation verdict rollup when available.
    # @param email_id [String] 
    # @return [EmailAuditAnalysisResult]
    def check_email_audit1(email_id : String)
      data, _status_code, _headers = check_email_audit1_with_http_info(email_id)
      data
    end

    # Run aggregate email audit for a stored email
    # Runs the same message-level audit bundle used by the email audit dashboard in one request. Combines content checks, HTML validation, compatibility analysis, and reputation verdict rollup when available.
    # @param email_id [String] 
    # @return [Array<(EmailAuditAnalysisResult, Integer, Hash)>] EmailAuditAnalysisResult data, response status code and response headers
    def check_email_audit1_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.check_email_audit1 ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.check_email_audit1")
      end
      # resource path
      local_var_path = "/emails/{emailId}/check-email-audit".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailAuditAnalysisResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.check_email_audit1",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#check_email_audit1\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailAuditAnalysisResult.from_json(data), status_code, headers
    end

    # Check email body for broken links, images, and spelling issues
    # Runs content quality checks against hydrated email body content. This endpoint performs outbound HTTP checks for linked resources, so avoid use with sensitive or stateful URLs.
    # @param email_id [String] 
    # @return [CheckEmailBodyResults]
    def check_email_body(email_id : String)
      data, _status_code, _headers = check_email_body_with_http_info(email_id)
      data
    end

    # Check email body for broken links, images, and spelling issues
    # Runs content quality checks against hydrated email body content. This endpoint performs outbound HTTP checks for linked resources, so avoid use with sensitive or stateful URLs.
    # @param email_id [String] 
    # @return [Array<(CheckEmailBodyResults, Integer, Hash)>] CheckEmailBodyResults data, response status code and response headers
    def check_email_body_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.check_email_body ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.check_email_body")
      end
      # resource path
      local_var_path = "/emails/{emailId}/check-email-body".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "CheckEmailBodyResults"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.check_email_body",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#check_email_body\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return CheckEmailBodyResults.from_json(data), status_code, headers
    end

    # Check client support for features used in a stored email body
    # Detects HTML/CSS features in the target email body and reports compatibility across major email clients and devices.
    # @param email_id [String] 
    # @return [CheckEmailBodyFeatureSupportResults]
    def check_email_body_feature_support(email_id : String)
      data, _status_code, _headers = check_email_body_feature_support_with_http_info(email_id)
      data
    end

    # Check client support for features used in a stored email body
    # Detects HTML/CSS features in the target email body and reports compatibility across major email clients and devices.
    # @param email_id [String] 
    # @return [Array<(CheckEmailBodyFeatureSupportResults, Integer, Hash)>] CheckEmailBodyFeatureSupportResults data, response status code and response headers
    def check_email_body_feature_support_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.check_email_body_feature_support ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.check_email_body_feature_support")
      end
      # resource path
      local_var_path = "/emails/{emailId}/check-email-body-feature-support".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "CheckEmailBodyFeatureSupportResults"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.check_email_body_feature_support",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#check_email_body_feature_support\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return CheckEmailBodyFeatureSupportResults.from_json(data), status_code, headers
    end

    # Check email-client support for a provided HTML body
    # Evaluates HTML/CSS features in the supplied body and reports support coverage across major email clients and platforms.
    # @param check_email_client_support_options [CheckEmailClientSupportOptions] 
    # @return [CheckEmailClientSupportResults]
    def check_email_client_support(check_email_client_support_options : CheckEmailClientSupportOptions)
      data, _status_code, _headers = check_email_client_support_with_http_info(check_email_client_support_options)
      data
    end

    # Check email-client support for a provided HTML body
    # Evaluates HTML/CSS features in the supplied body and reports support coverage across major email clients and platforms.
    # @param check_email_client_support_options [CheckEmailClientSupportOptions] 
    # @return [Array<(CheckEmailClientSupportResults, Integer, Hash)>] CheckEmailClientSupportResults data, response status code and response headers
    def check_email_client_support_with_http_info(check_email_client_support_options : CheckEmailClientSupportOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.check_email_client_support ..."}
      end
      # verify the required parameter "check_email_client_support_options" is set
      if @api_client.config.client_side_validation && check_email_client_support_options.nil?
        raise ArgumentError.new("Missing the required parameter 'check_email_client_support_options' when calling EmailControllerApi.check_email_client_support")
      end
      # resource path
      local_var_path = "/emails/check-email-client-support"

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = check_email_client_support_options.to_json

      # return_type
      return_type = "CheckEmailClientSupportResults"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.check_email_client_support",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#check_email_client_support\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return CheckEmailClientSupportResults.from_json(data), status_code, headers
    end

    # Persist aggregate email audit for a stored email
    # Runs the aggregate audit bundle for the target email and stores the resulting audit record for later review and history tracking.
    # @param email_id [String] 
    # @return [EmailAuditDto]
    def create_email_audit_for_email(email_id : String)
      data, _status_code, _headers = create_email_audit_for_email_with_http_info(email_id)
      data
    end

    # Persist aggregate email audit for a stored email
    # Runs the aggregate audit bundle for the target email and stores the resulting audit record for later review and history tracking.
    # @param email_id [String] 
    # @return [Array<(EmailAuditDto, Integer, Hash)>] EmailAuditDto data, response status code and response headers
    def create_email_audit_for_email_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.create_email_audit_for_email ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.create_email_audit_for_email")
      end
      # resource path
      local_var_path = "/emails/{emailId}/audit".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailAuditDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.create_email_audit_for_email",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#create_email_audit_for_email\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailAuditDto.from_json(data), status_code, headers
    end

    # Delete all emails in all inboxes.
    # Deletes all emails for the authenticated account context. This operation is destructive and cannot be undone.
    # @return [nil]
    def delete_all_emails()
      delete_all_emails_with_http_info()
      nil
    end

    # Delete all emails in all inboxes.
    # Deletes all emails for the authenticated account context. This operation is destructive and cannot be undone.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def delete_all_emails_with_http_info()
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.delete_all_emails ..."}
      end
      # resource path
      local_var_path = "/emails"

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:DELETE,
                                                        local_var_path,
                                                        :"EmailControllerApi.delete_all_emails",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#delete_all_emails\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end

    # Delete an email
    # Deletes a single email from account scope. Operation is destructive and not reversible.
    # @param email_id [String] ID of email to delete
    # @return [nil]
    def delete_email(email_id : String)
      delete_email_with_http_info(email_id)
      nil
    end

    # Delete an email
    # Deletes a single email from account scope. Operation is destructive and not reversible.
    # @param email_id [String] ID of email to delete
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def delete_email_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.delete_email ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.delete_email")
      end
      # resource path
      local_var_path = "/emails/{emailId}".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:DELETE,
                                                        local_var_path,
                                                        :"EmailControllerApi.delete_email",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#delete_email\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end

    # Get email attachment bytes. Returned as `octet-stream` with content type header. If you have trouble with byte responses try the `downloadAttachmentBase64` response endpoints and convert the base 64 encoded content to a file or string.
    # Returns attachment bytes by attachment ID. Use attachment IDs from email payloads or attachment listing endpoints.
    # @param email_id [String] ID of email
    # @param attachment_id [String] ID of attachment
    # @return [String]
    def download_attachment(email_id : String, attachment_id : String, api_key : String?)
      data, _status_code, _headers = download_attachment_with_http_info(email_id, attachment_id, api_key)
      data
    end

    # Get email attachment bytes. Returned as &#x60;octet-stream&#x60; with content type header. If you have trouble with byte responses try the &#x60;downloadAttachmentBase64&#x60; response endpoints and convert the base 64 encoded content to a file or string.
    # Returns attachment bytes by attachment ID. Use attachment IDs from email payloads or attachment listing endpoints.
    # @param email_id [String] ID of email
    # @param attachment_id [String] ID of attachment
    # @return [Array<(String, Integer, Hash)>] String data, response status code and response headers
    def download_attachment_with_http_info(email_id : String, attachment_id : String, api_key : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.download_attachment ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.download_attachment")
      end
      # verify the required parameter "attachment_id" is set
      if @api_client.config.client_side_validation && attachment_id.nil?
        raise ArgumentError.new("Missing the required parameter 'attachment_id' when calling EmailControllerApi.download_attachment")
      end
      # resource path
      local_var_path = "/emails/{emailId}/attachments/{attachmentId}".sub("{" + "emailId" + "}", URI.encode(email_id.to_s)).sub("{" + "attachmentId" + "}", URI.encode(attachment_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"apiKey"] = api_key

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/octet-stream"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "String"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.download_attachment",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#download_attachment\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return String.from_json(data), status_code, headers
    end

    # Get email attachment as base64 encoded string as an alternative to binary responses. Decode the `base64FileContents` as a `utf-8` encoded string or array of bytes depending on the `contentType`.
    # Returns attachment payload as base64 in JSON. Useful for clients that cannot reliably consume binary streaming responses.
    # @param email_id [String] ID of email
    # @param attachment_id [String] ID of attachment
    # @return [DownloadAttachmentDto]
    def download_attachment_base64(email_id : String, attachment_id : String)
      data, _status_code, _headers = download_attachment_base64_with_http_info(email_id, attachment_id)
      data
    end

    # Get email attachment as base64 encoded string as an alternative to binary responses. Decode the &#x60;base64FileContents&#x60; as a &#x60;utf-8&#x60; encoded string or array of bytes depending on the &#x60;contentType&#x60;.
    # Returns attachment payload as base64 in JSON. Useful for clients that cannot reliably consume binary streaming responses.
    # @param email_id [String] ID of email
    # @param attachment_id [String] ID of attachment
    # @return [Array<(DownloadAttachmentDto, Integer, Hash)>] DownloadAttachmentDto data, response status code and response headers
    def download_attachment_base64_with_http_info(email_id : String, attachment_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.download_attachment_base64 ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.download_attachment_base64")
      end
      # verify the required parameter "attachment_id" is set
      if @api_client.config.client_side_validation && attachment_id.nil?
        raise ArgumentError.new("Missing the required parameter 'attachment_id' when calling EmailControllerApi.download_attachment_base64")
      end
      # resource path
      local_var_path = "/emails/{emailId}/attachments/{attachmentId}/base64".sub("{" + "emailId" + "}", URI.encode(email_id.to_s)).sub("{" + "attachmentId" + "}", URI.encode(attachment_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "DownloadAttachmentDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.download_attachment_base64",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#download_attachment_base64\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return DownloadAttachmentDto.from_json(data), status_code, headers
    end

    # Get email body as string. Returned as `plain/text` with content type header.
    # Returns hydrated email body text as a string with content type aligned to the underlying body format.
    # @param email_id [String] ID of email
    # @return [String]
    def download_body(email_id : String)
      data, _status_code, _headers = download_body_with_http_info(email_id)
      data
    end

    # Get email body as string. Returned as &#x60;plain/text&#x60; with content type header.
    # Returns hydrated email body text as a string with content type aligned to the underlying body format.
    # @param email_id [String] ID of email
    # @return [Array<(String, Integer, Hash)>] String data, response status code and response headers
    def download_body_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.download_body ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.download_body")
      end
      # resource path
      local_var_path = "/emails/{emailId}/body".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["text/plain", "text/html"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "String"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.download_body",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#download_body\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return String.from_json(data), status_code, headers
    end

    # Get email body in bytes. Returned as `octet-stream` with content type header.
    # Streams hydrated email body bytes with content type derived from the message body format.
    # @param email_id [String] ID of email
    # @return [String]
    def download_body_bytes(email_id : String)
      data, _status_code, _headers = download_body_bytes_with_http_info(email_id)
      data
    end

    # Get email body in bytes. Returned as &#x60;octet-stream&#x60; with content type header.
    # Streams hydrated email body bytes with content type derived from the message body format.
    # @param email_id [String] ID of email
    # @return [Array<(String, Integer, Hash)>] String data, response status code and response headers
    def download_body_bytes_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.download_body_bytes ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.download_body_bytes")
      end
      # resource path
      local_var_path = "/emails/{emailId}/body-bytes".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/octet-stream"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "String"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.download_body_bytes",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#download_body_bytes\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return String.from_json(data), status_code, headers
    end

    # Forward email to recipients
    # Forwards an existing email to new recipients. Uses the owning inbox context unless overridden by allowed sender options.
    # @param email_id [String] ID of email
    # @param forward_email_options [ForwardEmailOptions] 
    # @return [SentEmailDto]
    def forward_email(email_id : String, forward_email_options : ForwardEmailOptions)
      data, _status_code, _headers = forward_email_with_http_info(email_id, forward_email_options)
      data
    end

    # Forward email to recipients
    # Forwards an existing email to new recipients. Uses the owning inbox context unless overridden by allowed sender options.
    # @param email_id [String] ID of email
    # @param forward_email_options [ForwardEmailOptions] 
    # @return [Array<(SentEmailDto, Integer, Hash)>] SentEmailDto data, response status code and response headers
    def forward_email_with_http_info(email_id : String, forward_email_options : ForwardEmailOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.forward_email ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.forward_email")
      end
      # verify the required parameter "forward_email_options" is set
      if @api_client.config.client_side_validation && forward_email_options.nil?
        raise ArgumentError.new("Missing the required parameter 'forward_email_options' when calling EmailControllerApi.forward_email")
      end
      # resource path
      local_var_path = "/emails/{emailId}/forward".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = forward_email_options.to_json

      # return_type
      return_type = "SentEmailDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.forward_email",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#forward_email\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return SentEmailDto.from_json(data), status_code, headers
    end

    # Get email attachment metadata. This is the `contentType` and `contentLength` of an attachment. To get the individual attachments  use the `downloadAttachment` methods.
    # Returns metadata for a specific attachment ID (name, content type, and size fields).
    # @param email_id [String] ID of email
    # @param attachment_id [String] ID of attachment
    # @return [AttachmentMetaData]
    def get_attachment_meta_data(email_id : String, attachment_id : String)
      data, _status_code, _headers = get_attachment_meta_data_with_http_info(email_id, attachment_id)
      data
    end

    # Get email attachment metadata. This is the &#x60;contentType&#x60; and &#x60;contentLength&#x60; of an attachment. To get the individual attachments  use the &#x60;downloadAttachment&#x60; methods.
    # Returns metadata for a specific attachment ID (name, content type, and size fields).
    # @param email_id [String] ID of email
    # @param attachment_id [String] ID of attachment
    # @return [Array<(AttachmentMetaData, Integer, Hash)>] AttachmentMetaData data, response status code and response headers
    def get_attachment_meta_data_with_http_info(email_id : String, attachment_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_attachment_meta_data ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_attachment_meta_data")
      end
      # verify the required parameter "attachment_id" is set
      if @api_client.config.client_side_validation && attachment_id.nil?
        raise ArgumentError.new("Missing the required parameter 'attachment_id' when calling EmailControllerApi.get_attachment_meta_data")
      end
      # resource path
      local_var_path = "/emails/{emailId}/attachments/{attachmentId}/metadata".sub("{" + "emailId" + "}", URI.encode(email_id.to_s)).sub("{" + "attachmentId" + "}", URI.encode(attachment_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "AttachmentMetaData"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_attachment_meta_data",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_attachment_meta_data\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return AttachmentMetaData.from_json(data), status_code, headers
    end

    # Get hydrated email (headers and body)
    # Returns parsed email content including headers and body fields. Accessing hydrated content may mark the email as read depending on read-state rules.
    # @param email_id [String] 
    # @return [Email]
    def get_email(email_id : String)
      data, _status_code, _headers = get_email_with_http_info(email_id)
      data
    end

    # Get hydrated email (headers and body)
    # Returns parsed email content including headers and body fields. Accessing hydrated content may mark the email as read depending on read-state rules.
    # @param email_id [String] 
    # @return [Array<(Email, Integer, Hash)>] Email data, response status code and response headers
    def get_email_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email")
      end
      # resource path
      local_var_path = "/emails/{emailId}".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json;charset=UTF-8", "application/xml;charset=UTF-8", "application/json; charset=UTF-8", "application/xml; charset=UTF-8"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Email"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Email.from_json(data), status_code, headers
    end

    # List attachment metadata for an email
    # Returns metadata for all attachment IDs associated with the email (name, content type, size, and IDs).
    # @param email_id [String] ID of email
    # @return [Array(AttachmentMetaData)]
    def get_email_attachments(email_id : String)
      data, _status_code, _headers = get_email_attachments_with_http_info(email_id)
      data
    end

    # List attachment metadata for an email
    # Returns metadata for all attachment IDs associated with the email (name, content type, size, and IDs).
    # @param email_id [String] ID of email
    # @return [Array<(Array(AttachmentMetaData), Integer, Hash)>] Array(AttachmentMetaData) data, response status code and response headers
    def get_email_attachments_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_attachments ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_attachments")
      end
      # resource path
      local_var_path = "/emails/{emailId}/attachments".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Array(AttachmentMetaData)"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_attachments",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_attachments\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Array(AttachmentMetaData).from_json(data), status_code, headers
    end

    # Extract verification codes from an email
    # Extracts one-time passcodes and similar tokens from email content using the selected extraction method and fallback options.
    # @param email_id [String] ID of email to extract codes from
    # @return [ExtractCodesResult]
    def get_email_codes(email_id : String, extract_codes_options : ExtractCodesOptions?)
      data, _status_code, _headers = get_email_codes_with_http_info(email_id, extract_codes_options)
      data
    end

    # Extract verification codes from an email
    # Extracts one-time passcodes and similar tokens from email content using the selected extraction method and fallback options.
    # @param email_id [String] ID of email to extract codes from
    # @return [Array<(ExtractCodesResult, Integer, Hash)>] ExtractCodesResult data, response status code and response headers
    def get_email_codes_with_http_info(email_id : String, extract_codes_options : ExtractCodesOptions?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_codes ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_codes")
      end
      # resource path
      local_var_path = "/emails/{emailId}/codes".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = extract_codes_options.to_json

      # return_type
      return_type = "ExtractCodesResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_codes",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_codes\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return ExtractCodesResult.from_json(data), status_code, headers
    end

    # Run regex against hydrated email body and return matches
    # Executes a Java regex pattern over hydrated email body text and returns the full match plus capture groups. Pattern syntax follows Java `Pattern` rules.
    # @param email_id [String] ID of email to match against
    # @param content_match_options [ContentMatchOptions] 
    # @return [EmailContentMatchResult]
    def get_email_content_match(email_id : String, content_match_options : ContentMatchOptions)
      data, _status_code, _headers = get_email_content_match_with_http_info(email_id, content_match_options)
      data
    end

    # Run regex against hydrated email body and return matches
    # Executes a Java regex pattern over hydrated email body text and returns the full match plus capture groups. Pattern syntax follows Java &#x60;Pattern&#x60; rules.
    # @param email_id [String] ID of email to match against
    # @param content_match_options [ContentMatchOptions] 
    # @return [Array<(EmailContentMatchResult, Integer, Hash)>] EmailContentMatchResult data, response status code and response headers
    def get_email_content_match_with_http_info(email_id : String, content_match_options : ContentMatchOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_content_match ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_content_match")
      end
      # verify the required parameter "content_match_options" is set
      if @api_client.config.client_side_validation && content_match_options.nil?
        raise ArgumentError.new("Missing the required parameter 'content_match_options' when calling EmailControllerApi.get_email_content_match")
      end
      # resource path
      local_var_path = "/emails/{emailId}/contentMatch".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = content_match_options.to_json

      # return_type
      return_type = "EmailContentMatchResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_content_match",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_content_match\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailContentMatchResult.from_json(data), status_code, headers
    end

    # Get email content part by content type
    # Extracts one MIME body part by `contentType` and optional `index`, returned in a structured DTO with metadata.
    # @param email_id [String] ID of email to match against
    # @param content_type [String] Content type
    # @return [EmailContentPartResult]
    def get_email_content_part(email_id : String, content_type : String, strict : Bool?, index : Int32?)
      data, _status_code, _headers = get_email_content_part_with_http_info(email_id, content_type, strict, index)
      data
    end

    # Get email content part by content type
    # Extracts one MIME body part by &#x60;contentType&#x60; and optional &#x60;index&#x60;, returned in a structured DTO with metadata.
    # @param email_id [String] ID of email to match against
    # @param content_type [String] Content type
    # @return [Array<(EmailContentPartResult, Integer, Hash)>] EmailContentPartResult data, response status code and response headers
    def get_email_content_part_with_http_info(email_id : String, content_type : String, strict : Bool?, index : Int32?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_content_part ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_content_part")
      end
      # verify the required parameter "content_type" is set
      if @api_client.config.client_side_validation && content_type.nil?
        raise ArgumentError.new("Missing the required parameter 'content_type' when calling EmailControllerApi.get_email_content_part")
      end
      # resource path
      local_var_path = "/emails/{emailId}/contentPart".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"contentType"] = content_type
      query_params[:"strict"] = strict
      query_params[:"index"] = index

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json;charset=UTF-8", "application/xml;charset=UTF-8", "application/json; charset=UTF-8", "application/xml; charset=UTF-8"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailContentPartResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_content_part",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_content_part\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailContentPartResult.from_json(data), status_code, headers
    end

    # Get multipart content part as raw response
    # Extracts one MIME body part by `contentType` and optional `index`, and returns raw content with matching response content type when valid.
    # @param email_id [String] ID of email to match against
    # @param content_type [String] Content type
    # @return [String]
    def get_email_content_part_content(email_id : String, content_type : String, strict : Bool?, index : Int32?)
      data, _status_code, _headers = get_email_content_part_content_with_http_info(email_id, content_type, strict, index)
      data
    end

    # Get multipart content part as raw response
    # Extracts one MIME body part by &#x60;contentType&#x60; and optional &#x60;index&#x60;, and returns raw content with matching response content type when valid.
    # @param email_id [String] ID of email to match against
    # @param content_type [String] Content type
    # @return [Array<(String, Integer, Hash)>] String data, response status code and response headers
    def get_email_content_part_content_with_http_info(email_id : String, content_type : String, strict : Bool?, index : Int32?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_content_part_content ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_content_part_content")
      end
      # verify the required parameter "content_type" is set
      if @api_client.config.client_side_validation && content_type.nil?
        raise ArgumentError.new("Missing the required parameter 'content_type' when calling EmailControllerApi.get_email_content_part_content")
      end
      # resource path
      local_var_path = "/emails/{emailId}/contentPart/raw".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"contentType"] = content_type
      query_params[:"strict"] = strict
      query_params[:"index"] = index

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "String"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_content_part_content",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_content_part_content\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return String.from_json(data), status_code, headers
    end

    # Get email count
    # Returns total email count for the authenticated user, or count scoped to a specific inbox when `inboxId` is provided.
    # @return [CountDto]
    def get_email_count(inbox_id : String?)
      data, _status_code, _headers = get_email_count_with_http_info(inbox_id)
      data
    end

    # Get email count
    # Returns total email count for the authenticated user, or count scoped to a specific inbox when &#x60;inboxId&#x60; is provided.
    # @return [Array<(CountDto, Integer, Hash)>] CountDto data, response status code and response headers
    def get_email_count_with_http_info(inbox_id : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_count ..."}
      end
      # resource path
      local_var_path = "/emails/emails/count"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxId"] = inbox_id

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "CountDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_count",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_count\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return CountDto.from_json(data), status_code, headers
    end

    # Get hydrated email HTML for browser rendering
    # Returns hydrated HTML body directly with `text/html` content type. Supports temporary access/browser usage and optional CID replacement for inline asset rendering.
    # @param email_id [String] 
    # @return [String]
    def get_email_html(email_id : String, replace_cid_images : Bool?)
      data, _status_code, _headers = get_email_html_with_http_info(email_id, replace_cid_images)
      data
    end

    # Get hydrated email HTML for browser rendering
    # Returns hydrated HTML body directly with &#x60;text/html&#x60; content type. Supports temporary access/browser usage and optional CID replacement for inline asset rendering.
    # @param email_id [String] 
    # @return [Array<(String, Integer, Hash)>] String data, response status code and response headers
    def get_email_html_with_http_info(email_id : String, replace_cid_images : Bool?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_html ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_html")
      end
      # resource path
      local_var_path = "/emails/{emailId}/html".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"replaceCidImages"] = replace_cid_images

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["text/html;charset=utf-8", "text/html"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "String"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_html",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_html\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return String.from_json(data), status_code, headers
    end

    # Get hydrated email HTML wrapped in JSON
    # Returns hydrated HTML body and subject in a JSON DTO. Useful for API clients that need structured response payloads instead of raw HTML responses.
    # @param email_id [String] 
    # @return [EmailHtmlDto]
    def get_email_html_json(email_id : String, replace_cid_images : Bool?)
      data, _status_code, _headers = get_email_html_json_with_http_info(email_id, replace_cid_images)
      data
    end

    # Get hydrated email HTML wrapped in JSON
    # Returns hydrated HTML body and subject in a JSON DTO. Useful for API clients that need structured response payloads instead of raw HTML responses.
    # @param email_id [String] 
    # @return [Array<(EmailHtmlDto, Integer, Hash)>] EmailHtmlDto data, response status code and response headers
    def get_email_html_json_with_http_info(email_id : String, replace_cid_images : Bool?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_html_json ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_html_json")
      end
      # resource path
      local_var_path = "/emails/{emailId}/html/json".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"replaceCidImages"] = replace_cid_images

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailHtmlDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_html_json",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_html_json\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailHtmlDto.from_json(data), status_code, headers
    end

    # Query hydrated HTML body and return matching text lines
    # Applies a JSoup/CSS selector to hydrated HTML email body and returns matching text lines.
    # @param email_id [String] ID of email to perform HTML query on
    # @param html_selector [String] HTML selector to search for. Uses JQuery/JSoup/CSS style selector like &#39;.my-div&#39; to match content. See https://jsoup.org/apidocs/org/jsoup/select/Selector.html for more information.
    # @return [EmailTextLinesResult]
    def get_email_html_query(email_id : String, html_selector : String)
      data, _status_code, _headers = get_email_html_query_with_http_info(email_id, html_selector)
      data
    end

    # Query hydrated HTML body and return matching text lines
    # Applies a JSoup/CSS selector to hydrated HTML email body and returns matching text lines.
    # @param email_id [String] ID of email to perform HTML query on
    # @param html_selector [String] HTML selector to search for. Uses JQuery/JSoup/CSS style selector like &#39;.my-div&#39; to match content. See https://jsoup.org/apidocs/org/jsoup/select/Selector.html for more information.
    # @return [Array<(EmailTextLinesResult, Integer, Hash)>] EmailTextLinesResult data, response status code and response headers
    def get_email_html_query_with_http_info(email_id : String, html_selector : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_html_query ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_html_query")
      end
      # verify the required parameter "html_selector" is set
      if @api_client.config.client_side_validation && html_selector.nil?
        raise ArgumentError.new("Missing the required parameter 'html_selector' when calling EmailControllerApi.get_email_html_query")
      end
      # resource path
      local_var_path = "/emails/{emailId}/htmlQuery".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"htmlSelector"] = html_selector

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailTextLinesResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_html_query",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_html_query\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailTextLinesResult.from_json(data), status_code, headers
    end

    # Extract links from an email HTML body
    # Parses HTML content and extracts link URLs (`href`). For non-HTML emails this endpoint returns a validation error.
    # @param email_id [String] ID of email to fetch text for
    # @return [EmailLinksResult]
    def get_email_links(email_id : String, selector : String?)
      data, _status_code, _headers = get_email_links_with_http_info(email_id, selector)
      data
    end

    # Extract links from an email HTML body
    # Parses HTML content and extracts link URLs (&#x60;href&#x60;). For non-HTML emails this endpoint returns a validation error.
    # @param email_id [String] ID of email to fetch text for
    # @return [Array<(EmailLinksResult, Integer, Hash)>] EmailLinksResult data, response status code and response headers
    def get_email_links_with_http_info(email_id : String, selector : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_links ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_links")
      end
      # resource path
      local_var_path = "/emails/{emailId}/links".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"selector"] = selector

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailLinksResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_links",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_links\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailLinksResult.from_json(data), status_code, headers
    end

    # Get email URLs for viewing in browser or downloading
    # Returns precomputed URLs for preview and raw message access for the specified email.
    # @param email_id [String] 
    # @return [EmailPreviewUrls]
    def get_email_preview_urls(email_id : String)
      data, _status_code, _headers = get_email_preview_urls_with_http_info(email_id)
      data
    end

    # Get email URLs for viewing in browser or downloading
    # Returns precomputed URLs for preview and raw message access for the specified email.
    # @param email_id [String] 
    # @return [Array<(EmailPreviewUrls, Integer, Hash)>] EmailPreviewUrls data, response status code and response headers
    def get_email_preview_urls_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_preview_urls ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_preview_urls")
      end
      # resource path
      local_var_path = "/emails/{emailId}/urls".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailPreviewUrls"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_preview_urls",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_preview_urls\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailPreviewUrls.from_json(data), status_code, headers
    end

    # Take a screenshot of an email in a browser and return base64 encoded string
    # Renders the email in a browser engine and returns PNG data as base64. Useful for APIs and dashboards that cannot easily stream binary responses.
    # @param email_id [String] 
    # @param get_email_screenshot_options [GetEmailScreenshotOptions] 
    # @return [EmailScreenshotResult]
    def get_email_screenshot_as_base64(email_id : String, get_email_screenshot_options : GetEmailScreenshotOptions)
      data, _status_code, _headers = get_email_screenshot_as_base64_with_http_info(email_id, get_email_screenshot_options)
      data
    end

    # Take a screenshot of an email in a browser and return base64 encoded string
    # Renders the email in a browser engine and returns PNG data as base64. Useful for APIs and dashboards that cannot easily stream binary responses.
    # @param email_id [String] 
    # @param get_email_screenshot_options [GetEmailScreenshotOptions] 
    # @return [Array<(EmailScreenshotResult, Integer, Hash)>] EmailScreenshotResult data, response status code and response headers
    def get_email_screenshot_as_base64_with_http_info(email_id : String, get_email_screenshot_options : GetEmailScreenshotOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_screenshot_as_base64 ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_screenshot_as_base64")
      end
      # verify the required parameter "get_email_screenshot_options" is set
      if @api_client.config.client_side_validation && get_email_screenshot_options.nil?
        raise ArgumentError.new("Missing the required parameter 'get_email_screenshot_options' when calling EmailControllerApi.get_email_screenshot_as_base64")
      end
      # resource path
      local_var_path = "/emails/{emailId}/screenshot/base64".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = get_email_screenshot_options.to_json

      # return_type
      return_type = "EmailScreenshotResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_screenshot_as_base64",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_screenshot_as_base64\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailScreenshotResult.from_json(data), status_code, headers
    end

    # Take a screenshot of an email in a browser
    # Renders the email in a browser engine and returns PNG bytes. Intended for visual QA and rendering regression checks.
    # @param email_id [String] 
    # @param get_email_screenshot_options [GetEmailScreenshotOptions] 
    # @return [nil]
    def get_email_screenshot_as_binary(email_id : String, get_email_screenshot_options : GetEmailScreenshotOptions)
      get_email_screenshot_as_binary_with_http_info(email_id, get_email_screenshot_options)
      nil
    end

    # Take a screenshot of an email in a browser
    # Renders the email in a browser engine and returns PNG bytes. Intended for visual QA and rendering regression checks.
    # @param email_id [String] 
    # @param get_email_screenshot_options [GetEmailScreenshotOptions] 
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def get_email_screenshot_as_binary_with_http_info(email_id : String, get_email_screenshot_options : GetEmailScreenshotOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_screenshot_as_binary ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_screenshot_as_binary")
      end
      # verify the required parameter "get_email_screenshot_options" is set
      if @api_client.config.client_side_validation && get_email_screenshot_options.nil?
        raise ArgumentError.new("Missing the required parameter 'get_email_screenshot_options' when calling EmailControllerApi.get_email_screenshot_as_binary")
      end
      # resource path
      local_var_path = "/emails/{emailId}/screenshot/binary".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = get_email_screenshot_options.to_json

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_screenshot_as_binary",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_screenshot_as_binary\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end

    # Extract signature from an inbound email
    # Attempts to parse a sender signature block from an email body. Uses raw MIME content parts when possible and falls back to hydrated body parsing. This is heuristic and may not be accurate for all email clients or formats.
    # @param email_id [String] ID of email to extract signature from
    # @return [EmailSignatureParseResult]
    def get_email_signature(email_id : String)
      data, _status_code, _headers = get_email_signature_with_http_info(email_id)
      data
    end

    # Extract signature from an inbound email
    # Attempts to parse a sender signature block from an email body. Uses raw MIME content parts when possible and falls back to hydrated body parsing. This is heuristic and may not be accurate for all email clients or formats.
    # @param email_id [String] ID of email to extract signature from
    # @return [Array<(EmailSignatureParseResult, Integer, Hash)>] EmailSignatureParseResult data, response status code and response headers
    def get_email_signature_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_signature ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_signature")
      end
      # resource path
      local_var_path = "/emails/{emailId}/signature".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailSignatureParseResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_signature",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_signature\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailSignatureParseResult.from_json(data), status_code, headers
    end

    # Get email summary (headers/metadata only)
    # Returns lightweight metadata and headers for an email. Use `getEmail` for hydrated body content or `getRawEmail` for original SMTP content.
    # @param email_id [String] 
    # @return [EmailPreview]
    def get_email_summary(email_id : String, decode : Bool?)
      data, _status_code, _headers = get_email_summary_with_http_info(email_id, decode)
      data
    end

    # Get email summary (headers/metadata only)
    # Returns lightweight metadata and headers for an email. Use &#x60;getEmail&#x60; for hydrated body content or &#x60;getRawEmail&#x60; for original SMTP content.
    # @param email_id [String] 
    # @return [Array<(EmailPreview, Integer, Hash)>] EmailPreview data, response status code and response headers
    def get_email_summary_with_http_info(email_id : String, decode : Bool?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_summary ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_summary")
      end
      # resource path
      local_var_path = "/emails/{emailId}/summary".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"decode"] = decode

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailPreview"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_summary",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_summary\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailPreview.from_json(data), status_code, headers
    end

    # Extract normalized text lines from email body
    # Converts email body content to line-based plain text with optional HTML entity decoding and custom line separator.
    # @param email_id [String] ID of email to fetch text for
    # @return [EmailTextLinesResult]
    def get_email_text_lines(email_id : String, decode_html_entities : Bool?, line_separator : String?)
      data, _status_code, _headers = get_email_text_lines_with_http_info(email_id, decode_html_entities, line_separator)
      data
    end

    # Extract normalized text lines from email body
    # Converts email body content to line-based plain text with optional HTML entity decoding and custom line separator.
    # @param email_id [String] ID of email to fetch text for
    # @return [Array<(EmailTextLinesResult, Integer, Hash)>] EmailTextLinesResult data, response status code and response headers
    def get_email_text_lines_with_http_info(email_id : String, decode_html_entities : Bool?, line_separator : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_text_lines ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_email_text_lines")
      end
      # resource path
      local_var_path = "/emails/{emailId}/textLines".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"decodeHtmlEntities"] = decode_html_entities
      query_params[:"lineSeparator"] = line_separator

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailTextLinesResult"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_text_lines",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_text_lines\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailTextLinesResult.from_json(data), status_code, headers
    end

    # Get email thread metadata by thread ID
    # Returns thread metadata built from RFC 5322 `Message-ID`, `In-Reply-To`, and `References` headers. Use `getEmailThreadItems` to fetch the thread messages.
    # @param thread_id [String] 
    # @return [EmailThreadDto]
    def get_email_thread(thread_id : String)
      data, _status_code, _headers = get_email_thread_with_http_info(thread_id)
      data
    end

    # Get email thread metadata by thread ID
    # Returns thread metadata built from RFC 5322 &#x60;Message-ID&#x60;, &#x60;In-Reply-To&#x60;, and &#x60;References&#x60; headers. Use &#x60;getEmailThreadItems&#x60; to fetch the thread messages.
    # @param thread_id [String] 
    # @return [Array<(EmailThreadDto, Integer, Hash)>] EmailThreadDto data, response status code and response headers
    def get_email_thread_with_http_info(thread_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_thread ..."}
      end
      # verify the required parameter "thread_id" is set
      if @api_client.config.client_side_validation && thread_id.nil?
        raise ArgumentError.new("Missing the required parameter 'thread_id' when calling EmailControllerApi.get_email_thread")
      end
      # resource path
      local_var_path = "/emails/threads/{threadId}".sub("{" + "threadId" + "}", URI.encode(thread_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailThreadDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_thread",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_thread\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailThreadDto.from_json(data), status_code, headers
    end

    # Get messages in a specific email thread
    # Returns all messages in a thread ordered by `createdAt` using the requested sort direction.
    # @param thread_id [String] 
    # @return [EmailThreadItemsDto]
    def get_email_thread_items(thread_id : String, sort : String?)
      data, _status_code, _headers = get_email_thread_items_with_http_info(thread_id, sort)
      data
    end

    # Get messages in a specific email thread
    # Returns all messages in a thread ordered by &#x60;createdAt&#x60; using the requested sort direction.
    # @param thread_id [String] 
    # @return [Array<(EmailThreadItemsDto, Integer, Hash)>] EmailThreadItemsDto data, response status code and response headers
    def get_email_thread_items_with_http_info(thread_id : String, sort : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_thread_items ..."}
      end
      # verify the required parameter "thread_id" is set
      if @api_client.config.client_side_validation && thread_id.nil?
        raise ArgumentError.new("Missing the required parameter 'thread_id' when calling EmailControllerApi.get_email_thread_items")
      end
      allowable_values = ["ASC", "DESC"]
      if @api_client.config.client_side_validation && sort && !allowable_values.includes?(sort)
        raise ArgumentError.new("invalid value for \"sort\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/emails/threads/{threadId}/items".sub("{" + "threadId" + "}", URI.encode(thread_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"sort"] = sort

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailThreadItemsDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_thread_items",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_thread_items\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailThreadItemsDto.from_json(data), status_code, headers
    end

    # List email threads in paginated form
    # Lists conversation threads inferred from `Message-ID`, `In-Reply-To`, and `References`. Supports filtering by inbox, search text, and time range.
    # @return [PageEmailThreadProjection]
    def get_email_threads(html_selector : String?, page : Int32?, size : Int32?, sort : String?, search_filter : String?, since : Time?, before : Time?)
      data, _status_code, _headers = get_email_threads_with_http_info(html_selector, page, size, sort, search_filter, since, before)
      data
    end

    # List email threads in paginated form
    # Lists conversation threads inferred from &#x60;Message-ID&#x60;, &#x60;In-Reply-To&#x60;, and &#x60;References&#x60;. Supports filtering by inbox, search text, and time range.
    # @return [Array<(PageEmailThreadProjection, Integer, Hash)>] PageEmailThreadProjection data, response status code and response headers
    def get_email_threads_with_http_info(html_selector : String?, page : Int32?, size : Int32?, sort : String?, search_filter : String?, since : Time?, before : Time?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_email_threads ..."}
      end
      if @api_client.config.client_side_validation && !size.nil? && size > 100
        raise ArgumentError.new("invalid value for \"size\" when calling EmailControllerApi.get_email_threads, must be smaller than or equal to 100.")
      end

      allowable_values = ["ASC", "DESC"]
      if @api_client.config.client_side_validation && sort && !allowable_values.includes?(sort)
        raise ArgumentError.new("invalid value for \"sort\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/emails/threads"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"htmlSelector"] = html_selector
      query_params[:"page"] = page
      query_params[:"size"] = size
      query_params[:"sort"] = sort
      query_params[:"searchFilter"] = search_filter
      query_params[:"since"] = since
      query_params[:"before"] = before

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "PageEmailThreadProjection"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_email_threads",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_email_threads\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return PageEmailThreadProjection.from_json(data), status_code, headers
    end

    # Get all emails in all inboxes in paginated form. Email API list all.
    # Offset-style pagination endpoint for listing emails across inboxes. Supports inbox filters, unread-only, search, date boundaries, favourites, connector sync, plus-address filtering, and explicit include IDs.
    # @return [PageEmailProjection]
    def get_emails_offset_paginated(inbox_id : Array(String)?, page : Int32?, size : Int32?, sort : String?, unread_only : Bool?, search_filter : String?, since : Time?, before : Time?, favourited : Bool?, sync_connectors : Bool?, plus_address_id : String?, _include : Array(String)?)
      data, _status_code, _headers = get_emails_offset_paginated_with_http_info(inbox_id, page, size, sort, unread_only, search_filter, since, before, favourited, sync_connectors, plus_address_id, _include)
      data
    end

    # Get all emails in all inboxes in paginated form. Email API list all.
    # Offset-style pagination endpoint for listing emails across inboxes. Supports inbox filters, unread-only, search, date boundaries, favourites, connector sync, plus-address filtering, and explicit include IDs.
    # @return [Array<(PageEmailProjection, Integer, Hash)>] PageEmailProjection data, response status code and response headers
    def get_emails_offset_paginated_with_http_info(inbox_id : Array(String)?, page : Int32?, size : Int32?, sort : String?, unread_only : Bool?, search_filter : String?, since : Time?, before : Time?, favourited : Bool?, sync_connectors : Bool?, plus_address_id : String?, _include : Array(String)?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_emails_offset_paginated ..."}
      end
      if @api_client.config.client_side_validation && !size.nil? && size > 100
        raise ArgumentError.new("invalid value for \"size\" when calling EmailControllerApi.get_emails_offset_paginated, must be smaller than or equal to 100.")
      end

      allowable_values = ["ASC", "DESC"]
      if @api_client.config.client_side_validation && sort && !allowable_values.includes?(sort)
        raise ArgumentError.new("invalid value for \"sort\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/emails/offset-paginated"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxId"] = @api_client.build_collection_param(inbox_id, :multi)
      query_params[:"page"] = page
      query_params[:"size"] = size
      query_params[:"sort"] = sort
      query_params[:"unreadOnly"] = unread_only
      query_params[:"searchFilter"] = search_filter
      query_params[:"since"] = since
      query_params[:"before"] = before
      query_params[:"favourited"] = favourited
      query_params[:"syncConnectors"] = sync_connectors
      query_params[:"plusAddressId"] = plus_address_id
      query_params[:"include"] = @api_client.build_collection_param(_include, :multi)

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "PageEmailProjection"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_emails_offset_paginated",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_emails_offset_paginated\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return PageEmailProjection.from_json(data), status_code, headers
    end

    # Get all emails in all inboxes in paginated form. Email API list all.
    # Primary paginated email listing endpoint. Returns emails across inboxes with support for inbox filters, unread-only, search, date boundaries, favourites, connector sync, and plus-address filtering.
    # @return [PageEmailProjection]
    def get_emails_paginated(inbox_id : Array(String)?, page : Int32?, size : Int32?, sort : String?, unread_only : Bool?, search_filter : String?, since : Time?, before : Time?, sync_connectors : Bool?, plus_address_id : String?, favourited : Bool?)
      data, _status_code, _headers = get_emails_paginated_with_http_info(inbox_id, page, size, sort, unread_only, search_filter, since, before, sync_connectors, plus_address_id, favourited)
      data
    end

    # Get all emails in all inboxes in paginated form. Email API list all.
    # Primary paginated email listing endpoint. Returns emails across inboxes with support for inbox filters, unread-only, search, date boundaries, favourites, connector sync, and plus-address filtering.
    # @return [Array<(PageEmailProjection, Integer, Hash)>] PageEmailProjection data, response status code and response headers
    def get_emails_paginated_with_http_info(inbox_id : Array(String)?, page : Int32?, size : Int32?, sort : String?, unread_only : Bool?, search_filter : String?, since : Time?, before : Time?, sync_connectors : Bool?, plus_address_id : String?, favourited : Bool?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_emails_paginated ..."}
      end
      if @api_client.config.client_side_validation && !size.nil? && size > 100
        raise ArgumentError.new("invalid value for \"size\" when calling EmailControllerApi.get_emails_paginated, must be smaller than or equal to 100.")
      end

      allowable_values = ["ASC", "DESC"]
      if @api_client.config.client_side_validation && sort && !allowable_values.includes?(sort)
        raise ArgumentError.new("invalid value for \"sort\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/emails"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxId"] = @api_client.build_collection_param(inbox_id, :multi)
      query_params[:"page"] = page
      query_params[:"size"] = size
      query_params[:"sort"] = sort
      query_params[:"unreadOnly"] = unread_only
      query_params[:"searchFilter"] = search_filter
      query_params[:"since"] = since
      query_params[:"before"] = before
      query_params[:"syncConnectors"] = sync_connectors
      query_params[:"plusAddressId"] = plus_address_id
      query_params[:"favourited"] = favourited

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "PageEmailProjection"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_emails_paginated",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_emails_paginated\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return PageEmailProjection.from_json(data), status_code, headers
    end

    # Get Gravatar URL for an email address
    # Builds a Gravatar image URL from the provided email address and optional size. This endpoint does not fetch image bytes; it only returns the computed URL.
    # @param email_address [String] 
    # @return [GravatarUrl]
    def get_gravatar_url_for_email_address(email_address : String, size : String?)
      data, _status_code, _headers = get_gravatar_url_for_email_address_with_http_info(email_address, size)
      data
    end

    # Get Gravatar URL for an email address
    # Builds a Gravatar image URL from the provided email address and optional size. This endpoint does not fetch image bytes; it only returns the computed URL.
    # @param email_address [String] 
    # @return [Array<(GravatarUrl, Integer, Hash)>] GravatarUrl data, response status code and response headers
    def get_gravatar_url_for_email_address_with_http_info(email_address : String, size : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_gravatar_url_for_email_address ..."}
      end
      # verify the required parameter "email_address" is set
      if @api_client.config.client_side_validation && email_address.nil?
        raise ArgumentError.new("Missing the required parameter 'email_address' when calling EmailControllerApi.get_gravatar_url_for_email_address")
      end
      # resource path
      local_var_path = "/emails/gravatarFor"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"emailAddress"] = email_address
      query_params[:"size"] = size

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "GravatarUrl"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_gravatar_url_for_email_address",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_gravatar_url_for_email_address\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return GravatarUrl.from_json(data), status_code, headers
    end

    # Get latest email in all inboxes. Most recently received.
    # Returns the most recently received email across all inboxes or an optional subset of inbox IDs.
    # @return [Email]
    def get_latest_email(inbox_ids : Array(String)?)
      data, _status_code, _headers = get_latest_email_with_http_info(inbox_ids)
      data
    end

    # Get latest email in all inboxes. Most recently received.
    # Returns the most recently received email across all inboxes or an optional subset of inbox IDs.
    # @return [Array<(Email, Integer, Hash)>] Email data, response status code and response headers
    def get_latest_email_with_http_info(inbox_ids : Array(String)?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_latest_email ..."}
      end
      # resource path
      local_var_path = "/emails/latest"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxIds"] = @api_client.build_collection_param(inbox_ids, :multi)

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Email"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_latest_email",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_latest_email\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Email.from_json(data), status_code, headers
    end

    # Get latest email in an inbox. Use `WaitForController` to get emails that may not have arrived yet.
    # Returns the newest email for the specified inbox ID. For polling/wait semantics use wait endpoints.
    # @param inbox_id [String] ID of the inbox you want to get the latest email from
    # @return [Email]
    def get_latest_email_in_inbox1(inbox_id : String)
      data, _status_code, _headers = get_latest_email_in_inbox1_with_http_info(inbox_id)
      data
    end

    # Get latest email in an inbox. Use &#x60;WaitForController&#x60; to get emails that may not have arrived yet.
    # Returns the newest email for the specified inbox ID. For polling/wait semantics use wait endpoints.
    # @param inbox_id [String] ID of the inbox you want to get the latest email from
    # @return [Array<(Email, Integer, Hash)>] Email data, response status code and response headers
    def get_latest_email_in_inbox1_with_http_info(inbox_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_latest_email_in_inbox1 ..."}
      end
      # verify the required parameter "inbox_id" is set
      if @api_client.config.client_side_validation && inbox_id.nil?
        raise ArgumentError.new("Missing the required parameter 'inbox_id' when calling EmailControllerApi.get_latest_email_in_inbox1")
      end
      # resource path
      local_var_path = "/emails/latestIn"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxId"] = inbox_id

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Email"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_latest_email_in_inbox1",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_latest_email_in_inbox1\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Email.from_json(data), status_code, headers
    end

    # List organization-visible emails
    # Returns paginated emails visible through organization/team access. Supports inbox filtering, unread-only filtering, search, favourites, plus-address filtering, and optional connector sync.
    # @return [PageEmailProjection]
    def get_organization_emails_paginated(inbox_id : Array(String)?, page : Int32?, size : Int32?, sort : String?, unread_only : Bool?, search_filter : String?, since : Time?, before : Time?, sync_connectors : Bool?, favourited : Bool?, plus_address_id : String?)
      data, _status_code, _headers = get_organization_emails_paginated_with_http_info(inbox_id, page, size, sort, unread_only, search_filter, since, before, sync_connectors, favourited, plus_address_id)
      data
    end

    # List organization-visible emails
    # Returns paginated emails visible through organization/team access. Supports inbox filtering, unread-only filtering, search, favourites, plus-address filtering, and optional connector sync.
    # @return [Array<(PageEmailProjection, Integer, Hash)>] PageEmailProjection data, response status code and response headers
    def get_organization_emails_paginated_with_http_info(inbox_id : Array(String)?, page : Int32?, size : Int32?, sort : String?, unread_only : Bool?, search_filter : String?, since : Time?, before : Time?, sync_connectors : Bool?, favourited : Bool?, plus_address_id : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_organization_emails_paginated ..."}
      end
      if @api_client.config.client_side_validation && !size.nil? && size > 100
        raise ArgumentError.new("invalid value for \"size\" when calling EmailControllerApi.get_organization_emails_paginated, must be smaller than or equal to 100.")
      end

      allowable_values = ["ASC", "DESC"]
      if @api_client.config.client_side_validation && sort && !allowable_values.includes?(sort)
        raise ArgumentError.new("invalid value for \"sort\", must be one of #{allowable_values}")
      end
      # resource path
      local_var_path = "/emails/organization"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxId"] = @api_client.build_collection_param(inbox_id, :multi)
      query_params[:"page"] = page
      query_params[:"size"] = size
      query_params[:"sort"] = sort
      query_params[:"unreadOnly"] = unread_only
      query_params[:"searchFilter"] = search_filter
      query_params[:"since"] = since
      query_params[:"before"] = before
      query_params[:"syncConnectors"] = sync_connectors
      query_params[:"favourited"] = favourited
      query_params[:"plusAddressId"] = plus_address_id

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "PageEmailProjection"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_organization_emails_paginated",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_organization_emails_paginated\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return PageEmailProjection.from_json(data), status_code, headers
    end

    # Get raw email string. Returns unparsed raw SMTP message with headers and body.
    # Returns the original unparsed SMTP/MIME message as `text/plain`. Use JSON variant if your client expects JSON transport.
    # @param email_id [String] ID of email
    # @return [nil]
    def get_raw_email_contents(email_id : String)
      get_raw_email_contents_with_http_info(email_id)
      nil
    end

    # Get raw email string. Returns unparsed raw SMTP message with headers and body.
    # Returns the original unparsed SMTP/MIME message as &#x60;text/plain&#x60;. Use JSON variant if your client expects JSON transport.
    # @param email_id [String] ID of email
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def get_raw_email_contents_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_raw_email_contents ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_raw_email_contents")
      end
      # resource path
      local_var_path = "/emails/{emailId}/raw".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_raw_email_contents",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_raw_email_contents\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end

    # Get raw email in JSON. Unparsed SMTP message in JSON wrapper format.
    # Returns the original unparsed SMTP/MIME message wrapped in a JSON DTO for API clients that avoid plain-text stream responses.
    # @param email_id [String] ID of email
    # @return [RawEmailJson]
    def get_raw_email_json(email_id : String)
      data, _status_code, _headers = get_raw_email_json_with_http_info(email_id)
      data
    end

    # Get raw email in JSON. Unparsed SMTP message in JSON wrapper format.
    # Returns the original unparsed SMTP/MIME message wrapped in a JSON DTO for API clients that avoid plain-text stream responses.
    # @param email_id [String] ID of email
    # @return [Array<(RawEmailJson, Integer, Hash)>] RawEmailJson data, response status code and response headers
    def get_raw_email_json_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_raw_email_json ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.get_raw_email_json")
      end
      # resource path
      local_var_path = "/emails/{emailId}/raw/json".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "RawEmailJson"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_raw_email_json",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_raw_email_json\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return RawEmailJson.from_json(data), status_code, headers
    end

    # Get unread email count
    # Returns unread email count. An email is considered read after dashboard/API retrieval depending on your read workflow.
    # @return [UnreadCount]
    def get_unread_email_count(inbox_id : String?)
      data, _status_code, _headers = get_unread_email_count_with_http_info(inbox_id)
      data
    end

    # Get unread email count
    # Returns unread email count. An email is considered read after dashboard/API retrieval depending on your read workflow.
    # @return [Array<(UnreadCount, Integer, Hash)>] UnreadCount data, response status code and response headers
    def get_unread_email_count_with_http_info(inbox_id : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.get_unread_email_count ..."}
      end
      # resource path
      local_var_path = "/emails/unreadCount"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxId"] = inbox_id

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "UnreadCount"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"EmailControllerApi.get_unread_email_count",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#get_unread_email_count\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return UnreadCount.from_json(data), status_code, headers
    end

    # Mark all emails as read or unread
    # Sets read state for all emails, optionally scoped to one inbox. Use `read=false` to reset unread state in bulk.
    # @return [nil]
    def mark_all_as_read(read : Bool?, inbox_id : String?)
      mark_all_as_read_with_http_info(read, inbox_id)
      nil
    end

    # Mark all emails as read or unread
    # Sets read state for all emails, optionally scoped to one inbox. Use &#x60;read&#x3D;false&#x60; to reset unread state in bulk.
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def mark_all_as_read_with_http_info(read : Bool?, inbox_id : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.mark_all_as_read ..."}
      end
      # resource path
      local_var_path = "/emails/read"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"read"] = read
      query_params[:"inboxId"] = inbox_id

      # header parameters
      header_params = Hash(String, String).new

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:PATCH,
                                                        local_var_path,
                                                        :"EmailControllerApi.mark_all_as_read",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#mark_all_as_read\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end

    # Mark an email as read or unread
    # Sets read state for one email. Useful when implementing custom mailbox workflows that treat viewed messages as unread.
    # @param email_id [String] 
    # @return [EmailPreview]
    def mark_as_read(email_id : String, read : Bool?)
      data, _status_code, _headers = mark_as_read_with_http_info(email_id, read)
      data
    end

    # Mark an email as read or unread
    # Sets read state for one email. Useful when implementing custom mailbox workflows that treat viewed messages as unread.
    # @param email_id [String] 
    # @return [Array<(EmailPreview, Integer, Hash)>] EmailPreview data, response status code and response headers
    def mark_as_read_with_http_info(email_id : String, read : Bool?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.mark_as_read ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.mark_as_read")
      end
      # resource path
      local_var_path = "/emails/{emailId}/read".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"read"] = read

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "EmailPreview"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:PATCH,
                                                        local_var_path,
                                                        :"EmailControllerApi.mark_as_read",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#mark_as_read\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return EmailPreview.from_json(data), status_code, headers
    end

    # Reply to an email
    # Sends a reply using the original conversation context (subject/thread headers). Reply target resolution honors sender/reply-to semantics.
    # @param email_id [String] ID of the email that should be replied to
    # @param reply_to_email_options [ReplyToEmailOptions] 
    # @return [SentEmailDto]
    def reply_to_email(email_id : String, reply_to_email_options : ReplyToEmailOptions)
      data, _status_code, _headers = reply_to_email_with_http_info(email_id, reply_to_email_options)
      data
    end

    # Reply to an email
    # Sends a reply using the original conversation context (subject/thread headers). Reply target resolution honors sender/reply-to semantics.
    # @param email_id [String] ID of the email that should be replied to
    # @param reply_to_email_options [ReplyToEmailOptions] 
    # @return [Array<(SentEmailDto, Integer, Hash)>] SentEmailDto data, response status code and response headers
    def reply_to_email_with_http_info(email_id : String, reply_to_email_options : ReplyToEmailOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.reply_to_email ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.reply_to_email")
      end
      # verify the required parameter "reply_to_email_options" is set
      if @api_client.config.client_side_validation && reply_to_email_options.nil?
        raise ArgumentError.new("Missing the required parameter 'reply_to_email_options' when calling EmailControllerApi.reply_to_email")
      end
      # resource path
      local_var_path = "/emails/{emailId}".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = reply_to_email_options.to_json

      # return_type
      return_type = "SentEmailDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:PUT,
                                                        local_var_path,
                                                        :"EmailControllerApi.reply_to_email",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#reply_to_email\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return SentEmailDto.from_json(data), status_code, headers
    end

    # Get all emails by search criteria. Return in paginated form.
    # Searches emails by sender/recipient/address/subject/id fields and returns paginated matches. Does not perform full-text body search.
    # @param search_emails_options [SearchEmailsOptions] 
    # @return [PageEmailProjection]
    def search_emails(search_emails_options : SearchEmailsOptions, sync_connectors : Bool?, favourited : Bool?, plus_address_id : String?)
      data, _status_code, _headers = search_emails_with_http_info(search_emails_options, sync_connectors, favourited, plus_address_id)
      data
    end

    # Get all emails by search criteria. Return in paginated form.
    # Searches emails by sender/recipient/address/subject/id fields and returns paginated matches. Does not perform full-text body search.
    # @param search_emails_options [SearchEmailsOptions] 
    # @return [Array<(PageEmailProjection, Integer, Hash)>] PageEmailProjection data, response status code and response headers
    def search_emails_with_http_info(search_emails_options : SearchEmailsOptions, sync_connectors : Bool?, favourited : Bool?, plus_address_id : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.search_emails ..."}
      end
      # verify the required parameter "search_emails_options" is set
      if @api_client.config.client_side_validation && search_emails_options.nil?
        raise ArgumentError.new("Missing the required parameter 'search_emails_options' when calling EmailControllerApi.search_emails")
      end
      # resource path
      local_var_path = "/emails/search"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"syncConnectors"] = sync_connectors
      query_params[:"favourited"] = favourited
      query_params[:"plusAddressId"] = plus_address_id

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = search_emails_options.to_json

      # return_type
      return_type = "PageEmailProjection"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.search_emails",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#search_emails\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return PageEmailProjection.from_json(data), status_code, headers
    end

    # Send email
    # Sends an email from an existing inbox, or creates a temporary inbox when `inboxId` is not provided. Supports `useDomainPool` and `virtualSend` inbox creation behavior for convenience sends.
    # @param send_email_options [SendEmailOptions] 
    # @return [nil]
    def send_email_source_optional(send_email_options : SendEmailOptions, inbox_id : String?, use_domain_pool : Bool?, virtual_send : Bool?)
      send_email_source_optional_with_http_info(send_email_options, inbox_id, use_domain_pool, virtual_send)
      nil
    end

    # Send email
    # Sends an email from an existing inbox, or creates a temporary inbox when &#x60;inboxId&#x60; is not provided. Supports &#x60;useDomainPool&#x60; and &#x60;virtualSend&#x60; inbox creation behavior for convenience sends.
    # @param send_email_options [SendEmailOptions] 
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def send_email_source_optional_with_http_info(send_email_options : SendEmailOptions, inbox_id : String?, use_domain_pool : Bool?, virtual_send : Bool?)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.send_email_source_optional ..."}
      end
      # verify the required parameter "send_email_options" is set
      if @api_client.config.client_side_validation && send_email_options.nil?
        raise ArgumentError.new("Missing the required parameter 'send_email_options' when calling EmailControllerApi.send_email_source_optional")
      end
      # resource path
      local_var_path = "/emails"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"inboxId"] = inbox_id
      query_params[:"useDomainPool"] = use_domain_pool
      query_params[:"virtualSend"] = virtual_send

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = send_email_options.to_json

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.send_email_source_optional",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#send_email_source_optional\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end

    # Set email favourited state
    # Sets favourite state for an email for dashboard/search workflows.
    # @param email_id [String] ID of email to set favourite state
    # @param favourited [Bool] 
    # @return [nil]
    def set_email_favourited(email_id : String, favourited : Bool)
      set_email_favourited_with_http_info(email_id, favourited)
      nil
    end

    # Set email favourited state
    # Sets favourite state for an email for dashboard/search workflows.
    # @param email_id [String] ID of email to set favourite state
    # @param favourited [Bool] 
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def set_email_favourited_with_http_info(email_id : String, favourited : Bool)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.set_email_favourited ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.set_email_favourited")
      end
      # verify the required parameter "favourited" is set
      if @api_client.config.client_side_validation && favourited.nil?
        raise ArgumentError.new("Missing the required parameter 'favourited' when calling EmailControllerApi.set_email_favourited")
      end
      # resource path
      local_var_path = "/emails/{emailId}/favourite".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"favourited"] = favourited

      # header parameters
      header_params = Hash(String, String).new

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:PUT,
                                                        local_var_path,
                                                        :"EmailControllerApi.set_email_favourited",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#set_email_favourited\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end

    # Validate email HTML contents
    # Runs HTML validation on the email body when HTML is present. Non-HTML emails are treated as valid for this check.
    # @param email_id [String] ID of email
    # @return [ValidationDto]
    def validate_email(email_id : String)
      data, _status_code, _headers = validate_email_with_http_info(email_id)
      data
    end

    # Validate email HTML contents
    # Runs HTML validation on the email body when HTML is present. Non-HTML emails are treated as valid for this check.
    # @param email_id [String] ID of email
    # @return [Array<(ValidationDto, Integer, Hash)>] ValidationDto data, response status code and response headers
    def validate_email_with_http_info(email_id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: EmailControllerApi.validate_email ..."}
      end
      # verify the required parameter "email_id" is set
      if @api_client.config.client_side_validation && email_id.nil?
        raise ArgumentError.new("Missing the required parameter 'email_id' when calling EmailControllerApi.validate_email")
      end
      # resource path
      local_var_path = "/emails/{emailId}/validate".sub("{" + "emailId" + "}", URI.encode(email_id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "ValidationDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"EmailControllerApi.validate_email",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: EmailControllerApi#validate_email\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return ValidationDto.from_json(data), status_code, headers
    end
  end
end
