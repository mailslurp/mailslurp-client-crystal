# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails from dynamically allocated email addresses. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://www.mailslurp.com/docs/) - [Examples](https://github.com/mailslurp/examples) repository 
#
#The version of the OpenAPI document: 6.5.2
#
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.0
#

require "uri"

module 
  class BulkActionsControllerApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Bulk create Inboxes (email addresses)
    # @param count [Int32] Number of inboxes to be created in bulk
    # @return [Array(Inbox)]
    def bulk_create_inboxes(count : Int32)
      data, _status_code, _headers = bulk_create_inboxes_with_http_info(count)
      data
    end

    # Bulk create Inboxes (email addresses)
    # @param count [Int32] Number of inboxes to be created in bulk
    # @return [Array<(Array(Inbox), Integer, Hash)>] Array(Inbox) data, response status code and response headers
    def bulk_create_inboxes_with_http_info(count : Int32)
      if @api_client.config.debugging
        Log.debug {"Calling API: BulkActionsControllerApi.bulk_create_inboxes ..."}
      end
      # verify the required parameter "count" is set
      if @api_client.config.client_side_validation && count.nil?
        raise ArgumentError.new("Missing the required parameter 'count' when calling BulkActionsControllerApi.bulk_create_inboxes")
      end
      # resource path
      local_var_path = "/bulk/inboxes"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"count"] = count

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "Array(Inbox)"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"BulkActionsControllerApi.bulk_create_inboxes",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: BulkActionsControllerApi#bulk_create_inboxes\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return Array(Inbox).from_json(data), status_code, headers
    end

    # Bulk Delete Inboxes
    # @param ids [Array(String)] ids
    # @return [nil]
    def bulk_delete_inboxes(ids : Array(String))
      bulk_delete_inboxes_with_http_info(ids)
      nil
    end

    # Bulk Delete Inboxes
    # @param ids [Array(String)] ids
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def bulk_delete_inboxes_with_http_info(ids : Array(String))
      if @api_client.config.debugging
        Log.debug {"Calling API: BulkActionsControllerApi.bulk_delete_inboxes ..."}
      end
      # verify the required parameter "ids" is set
      if @api_client.config.client_side_validation && ids.nil?
        raise ArgumentError.new("Missing the required parameter 'ids' when calling BulkActionsControllerApi.bulk_delete_inboxes")
      end
      # resource path
      local_var_path = "/bulk/inboxes"

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = ids.to_json

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:DELETE,
                                                        local_var_path,
                                                        :"BulkActionsControllerApi.bulk_delete_inboxes",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: BulkActionsControllerApi#bulk_delete_inboxes\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end

    # Bulk Send Emails
    # @param bulk_send_email_options [BulkSendEmailOptions] bulkSendEmailOptions
    # @return [nil]
    def bulk_send_emails(bulk_send_email_options : BulkSendEmailOptions)
      bulk_send_emails_with_http_info(bulk_send_email_options)
      nil
    end

    # Bulk Send Emails
    # @param bulk_send_email_options [BulkSendEmailOptions] bulkSendEmailOptions
    # @return [Array<(nil, Integer, Hash)>] nil, response status code and response headers
    def bulk_send_emails_with_http_info(bulk_send_email_options : BulkSendEmailOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: BulkActionsControllerApi.bulk_send_emails ..."}
      end
      # verify the required parameter "bulk_send_email_options" is set
      if @api_client.config.client_side_validation && bulk_send_email_options.nil?
        raise ArgumentError.new("Missing the required parameter 'bulk_send_email_options' when calling BulkActionsControllerApi.bulk_send_emails")
      end
      # resource path
      local_var_path = "/bulk/send"

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = bulk_send_email_options.to_json

      # return_type
      return_type = nil

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"BulkActionsControllerApi.bulk_send_emails",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: BulkActionsControllerApi#bulk_send_emails\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return nil, status_code, headers
    end
  end
end
