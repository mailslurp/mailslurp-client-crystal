# #MailSlurp API
#
##MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
#
#The version of the OpenAPI document: 6.5.2
#Contact: contact@mailslurp.dev
#Generated by: https://openapi-generator.tech
#OpenAPI Generator version: 5.1.1
#

require "uri"

module 
  class MFAControllerApi
    property api_client : ApiClient

    def initialize(api_client = ApiClient.default)
      @api_client = api_client
    end
    # Create a TOTP device from an base32 secret key
    # Create a virtual TOTP device for a given secret key. This is usually present as an alternative login option when pairing OTP devices.
    # @param create_totp_device_base32_secret_key_options [CreateTotpDeviceBase32SecretKeyOptions] 
    # @return [TotpDeviceDto]
    def create_totp_device_for_base32_secret_key(create_totp_device_base32_secret_key_options : CreateTotpDeviceBase32SecretKeyOptions)
      data, _status_code, _headers = create_totp_device_for_base32_secret_key_with_http_info(create_totp_device_base32_secret_key_options)
      data
    end

    # Create a TOTP device from an base32 secret key
    # Create a virtual TOTP device for a given secret key. This is usually present as an alternative login option when pairing OTP devices.
    # @param create_totp_device_base32_secret_key_options [CreateTotpDeviceBase32SecretKeyOptions] 
    # @return [Array<(TotpDeviceDto, Integer, Hash)>] TotpDeviceDto data, response status code and response headers
    def create_totp_device_for_base32_secret_key_with_http_info(create_totp_device_base32_secret_key_options : CreateTotpDeviceBase32SecretKeyOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: MFAControllerApi.create_totp_device_for_base32_secret_key ..."}
      end
      # verify the required parameter "create_totp_device_base32_secret_key_options" is set
      if @api_client.config.client_side_validation && create_totp_device_base32_secret_key_options.nil?
        raise ArgumentError.new("Missing the required parameter 'create_totp_device_base32_secret_key_options' when calling MFAControllerApi.create_totp_device_for_base32_secret_key")
      end
      # resource path
      local_var_path = "/mfa/totp/device/base32SecretKey"

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = create_totp_device_base32_secret_key_options.to_json

      # return_type
      return_type = "TotpDeviceDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"MFAControllerApi.create_totp_device_for_base32_secret_key",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: MFAControllerApi#create_totp_device_for_base32_secret_key\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return TotpDeviceDto.from_json(data), status_code, headers
    end

    # Create a TOTP device from custom options
    # Create a virtual TOTP device for custom options specifying all parameters of the TOTP device.
    # @param create_totp_device_custom_options [CreateTotpDeviceCustomOptions] 
    # @return [TotpDeviceDto]
    def create_totp_device_for_custom(create_totp_device_custom_options : CreateTotpDeviceCustomOptions)
      data, _status_code, _headers = create_totp_device_for_custom_with_http_info(create_totp_device_custom_options)
      data
    end

    # Create a TOTP device from custom options
    # Create a virtual TOTP device for custom options specifying all parameters of the TOTP device.
    # @param create_totp_device_custom_options [CreateTotpDeviceCustomOptions] 
    # @return [Array<(TotpDeviceDto, Integer, Hash)>] TotpDeviceDto data, response status code and response headers
    def create_totp_device_for_custom_with_http_info(create_totp_device_custom_options : CreateTotpDeviceCustomOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: MFAControllerApi.create_totp_device_for_custom ..."}
      end
      # verify the required parameter "create_totp_device_custom_options" is set
      if @api_client.config.client_side_validation && create_totp_device_custom_options.nil?
        raise ArgumentError.new("Missing the required parameter 'create_totp_device_custom_options' when calling MFAControllerApi.create_totp_device_for_custom")
      end
      # resource path
      local_var_path = "/mfa/totp/device/custom"

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = create_totp_device_custom_options.to_json

      # return_type
      return_type = "TotpDeviceDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"MFAControllerApi.create_totp_device_for_custom",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: MFAControllerApi#create_totp_device_for_custom\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return TotpDeviceDto.from_json(data), status_code, headers
    end

    # Create a TOTP device from an OTP Auth URL
    # Create a virtual TOTP device for a given OTP Auth URL such as otpauth://totp/MyApp:alice@example.com?secret=ABC123&issuer=MyApp&period=30&digits=6&algorithm=SHA1. You can provider overrides in the options for each component of the URL.
    # @param create_totp_device_otp_auth_url_options [CreateTotpDeviceOtpAuthUrlOptions] 
    # @return [TotpDeviceDto]
    def create_totp_device_for_otp_auth_url(create_totp_device_otp_auth_url_options : CreateTotpDeviceOtpAuthUrlOptions)
      data, _status_code, _headers = create_totp_device_for_otp_auth_url_with_http_info(create_totp_device_otp_auth_url_options)
      data
    end

    # Create a TOTP device from an OTP Auth URL
    # Create a virtual TOTP device for a given OTP Auth URL such as otpauth://totp/MyApp:alice@example.com?secret&#x3D;ABC123&amp;issuer&#x3D;MyApp&amp;period&#x3D;30&amp;digits&#x3D;6&amp;algorithm&#x3D;SHA1. You can provider overrides in the options for each component of the URL.
    # @param create_totp_device_otp_auth_url_options [CreateTotpDeviceOtpAuthUrlOptions] 
    # @return [Array<(TotpDeviceDto, Integer, Hash)>] TotpDeviceDto data, response status code and response headers
    def create_totp_device_for_otp_auth_url_with_http_info(create_totp_device_otp_auth_url_options : CreateTotpDeviceOtpAuthUrlOptions)
      if @api_client.config.debugging
        Log.debug {"Calling API: MFAControllerApi.create_totp_device_for_otp_auth_url ..."}
      end
      # verify the required parameter "create_totp_device_otp_auth_url_options" is set
      if @api_client.config.client_side_validation && create_totp_device_otp_auth_url_options.nil?
        raise ArgumentError.new("Missing the required parameter 'create_totp_device_otp_auth_url_options' when calling MFAControllerApi.create_totp_device_for_otp_auth_url")
      end
      # resource path
      local_var_path = "/mfa/totp/device/otpAuthUrl"

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])
      # HTTP header "Content-Type"
      header_params["Content-Type"] = @api_client.select_header_content_type(["application/json"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = create_totp_device_otp_auth_url_options.to_json

      # return_type
      return_type = "TotpDeviceDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:POST,
                                                        local_var_path,
                                                        :"MFAControllerApi.create_totp_device_for_otp_auth_url",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: MFAControllerApi#create_totp_device_for_otp_auth_url\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return TotpDeviceDto.from_json(data), status_code, headers
    end

    # Get a TOTP device by ID
    # Get Time-Based One-Time Password (TOTP) device by its ID.
    # @param id [String] 
    # @return [TotpDeviceDto]
    def get_totp_device(id : String)
      data, _status_code, _headers = get_totp_device_with_http_info(id)
      data
    end

    # Get a TOTP device by ID
    # Get Time-Based One-Time Password (TOTP) device by its ID.
    # @param id [String] 
    # @return [Array<(TotpDeviceDto, Integer, Hash)>] TotpDeviceDto data, response status code and response headers
    def get_totp_device_with_http_info(id : String)
      if @api_client.config.debugging
        Log.debug {"Calling API: MFAControllerApi.get_totp_device ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling MFAControllerApi.get_totp_device")
      end
      # resource path
      local_var_path = "/mfa/totp/device/{id}".sub("{" + "id" + "}", URI.encode(id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "TotpDeviceDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"MFAControllerApi.get_totp_device",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: MFAControllerApi#get_totp_device\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return TotpDeviceDto.from_json(data), status_code, headers
    end

    # Get a TOTP device by username, issuer, or name. Returns empty if not found.
    # Get Time-Based One-Time Password (TOTP) device by its username and issuer mapping.
    # @return [TotpDeviceOptionalDto]
    def get_totp_device_by(name : String?, issuer : String?, username : String?)
      data, _status_code, _headers = get_totp_device_by_with_http_info(name, issuer, username)
      data
    end

    # Get a TOTP device by username, issuer, or name. Returns empty if not found.
    # Get Time-Based One-Time Password (TOTP) device by its username and issuer mapping.
    # @return [Array<(TotpDeviceOptionalDto, Integer, Hash)>] TotpDeviceOptionalDto data, response status code and response headers
    def get_totp_device_by_with_http_info(name : String?, issuer : String?, username : String?)
      if @api_client.config.debugging
        Log.debug {"Calling API: MFAControllerApi.get_totp_device_by ..."}
      end
      # resource path
      local_var_path = "/mfa/totp/device/by"

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"name"] = name
      query_params[:"issuer"] = issuer
      query_params[:"username"] = username

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "TotpDeviceOptionalDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"MFAControllerApi.get_totp_device_by",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: MFAControllerApi#get_totp_device_by\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return TotpDeviceOptionalDto.from_json(data), status_code, headers
    end

    # Get a TOTP device code by device ID
    # Get Time-Based One-Time Password for a device by its ID.
    # @param id [String] ID of the TOTP device to get the code for
    # @return [TotpDeviceCodeDto]
    def get_totp_device_code(id : String, at : Time?, min_seconds_until_expire : Int32?)
      data, _status_code, _headers = get_totp_device_code_with_http_info(id, at, min_seconds_until_expire)
      data
    end

    # Get a TOTP device code by device ID
    # Get Time-Based One-Time Password for a device by its ID.
    # @param id [String] ID of the TOTP device to get the code for
    # @return [Array<(TotpDeviceCodeDto, Integer, Hash)>] TotpDeviceCodeDto data, response status code and response headers
    def get_totp_device_code_with_http_info(id : String, at : Time?, min_seconds_until_expire : Int32?)
      if @api_client.config.debugging
        Log.debug {"Calling API: MFAControllerApi.get_totp_device_code ..."}
      end
      # verify the required parameter "id" is set
      if @api_client.config.client_side_validation && id.nil?
        raise ArgumentError.new("Missing the required parameter 'id' when calling MFAControllerApi.get_totp_device_code")
      end
      if @api_client.config.client_side_validation && !min_seconds_until_expire.nil? && min_seconds_until_expire > 30
        raise ArgumentError.new("invalid value for \"min_seconds_until_expire\" when calling MFAControllerApi.get_totp_device_code, must be smaller than or equal to 30.")
      end

      if @api_client.config.client_side_validation && !min_seconds_until_expire.nil? && min_seconds_until_expire < 1
        raise ArgumentError.new("invalid value for \"min_seconds_until_expire\" when calling MFAControllerApi.get_totp_device_code, must be greater than or equal to 1.")
      end

      # resource path
      local_var_path = "/mfa/totp/device/{id}/code".sub("{" + "id" + "}", URI.encode(id.to_s))

      # query parameters
      query_params = Hash(Symbol, String).new
      query_params[:"at"] = at
      query_params[:"minSecondsUntilExpire"] = min_seconds_until_expire

      # header parameters
      header_params = Hash(String, String).new
      # HTTP header "Accept" (if needed)
      header_params["Accept"] = @api_client.select_header_accept(["*/*"])

      # form parameters
      form_params = Hash(Symbol, String).new

      # http body (model)
      post_body = nil

      # return_type
      return_type = "TotpDeviceCodeDto"

      # auth_names
      auth_names = ["API_KEY"]

      data, status_code, headers = @api_client.call_api(:GET,
                                                        local_var_path,
                                                        :"MFAControllerApi.get_totp_device_code",
                                                        return_type,
                                                        post_body,
                                                        auth_names,
                                                        header_params,
                                                        query_params,
                                                        form_params)
      if @api_client.config.debugging
        Log.debug {"API called: MFAControllerApi#get_totp_device_code\nData: #{data.inspect}\nStatus code: #{status_code}\nHeaders: #{headers}"}
      end
      return TotpDeviceCodeDto.from_json(data), status_code, headers
    end
  end
end
